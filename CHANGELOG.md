# Changelog

本项目遵循语义化版本的方向记录公开发布变化。开源前的内部阶段记录保留在 `docs/`。

## [Unreleased]

- 修复上一轮取证修复（`e0b3b09`）自身引入的 P0：Storage 资源清理的"引用集完整性"判据两侧计数不同源——快照子集合同时存放实体文档与复习事件文档，服务器原始计数含两者，而容错实体解析丢弃复习事件，导致任何含复习事件的真实快照都让清理永久跳过（自动后台与手动强制清理双双静默失效，孤儿资源只增不删）。现按恢复路径同一前缀判据分区，两侧同源计数，清理恢复正常；畸形文档保护不变。说明见 [云同步取证修复与收口](docs/cloud-sync-forensic-remediation-2026-09-24.md) 的"后续修复"。这些源码修复尚未发布为 APK/EXE。
- 修复云同步取证审计发现的六处残留缺口：Storage 资源清理在云端状态不可信、引用集无法证明完整或存在未确认写入时一律跳过而非误删；通用回退 MIME 类型的豁免改为对称，字节相同的资源不再因传输层报告 `application/octet-stream` 而被永久拒收；"以本机为准"在无法证明云端集合完整时跳过创建"冲突前的云端版本"恢复点并诚实告警，绝不伪造声称完整的恢复点；增量拉取丢弃畸形文档时冻结完整性声明（拉取游标仍前进），避免后续破坏性入口信任缩水的数据集；外部化空载荷不再被当作有效合并祖先，云端已删字段不会复活。对 5 份真实备份的只读重放测得严格解析过度拒绝率为 0.0000%。说明见 [云同步取证修复与收口](docs/cloud-sync-forensic-remediation-2026-09-24.md)。这些源码修复尚未发布为 APK/EXE。
- 修复云同步软删除保留、部分提交核对与拉取游标隔离，并阻止旧助教回调、过期编辑器保存和恢复后的语音检查点覆盖有效状态；真实双端修改仍保留冲突保护。
- 修复今日计划后台回收使用旧副本导致删除复活、覆盖新关联的问题；普通保存不能隐式恢复删除计划。过期保存提供核对指引，草稿保存失败不再声称已落盘。白盒断言与报告勘误见 [复核修复与验收](docs/audit/cloud-sync-whitebox-remediation-2026-09-21.md)。这些源码修复尚未发布为 APK/EXE。

- 继续修复学习助教的跨端隐式写入：同步来的反馈不自动重复调用 AI，未完成整理提供手动入口，旧 AI 回调通过事务内快照校验避免覆盖同步结果；延迟复查保留活动任务关联，按窗口生成稳定身份，重复状态操作不改写完成证据时间。
- 修复学习助教运行时把本机模型绑定反复写入可同步角色配置的问题；实际请求仍使用本机模型，已有角色超时从仓库读取，正式学习结果继续同步。
- 修复应用启动自动选择当前训练任务，以及重复队列操作改写语义时间戳造成额外同步变化的问题；真正的队列/策略冲突仍保留人工处理保护。说明见 [助教同步修复](docs/cloud-sync-coach-write-boundaries-2026-09-21.md)。

## [0.2.5] - 2026-09-21

- 修复语音 ASR 终止帧缺失、部分识别结果丢失、重试未重新打开采集与错误诊断；仍保留真机 ASR、多轮弱网等验收门槛。
- 修复百炼 Qwen Audio 3.1/3.0 的 TTS 请求协议、音色与 HTTPS 下载；保留有限分段预取、通话内缓存重播和挂断清理。三款替代 TTS 的受控 Android 验收范围见真机记录。
- 改善 Android 学科选择菜单的可读性与命中区域，保持确认学科后才创建日志的语义。
- 按 APP 内九章使用教程重写 README，补齐今日计划、学习助教、AI/语音/播客、备份边界、常见问题和流程插图。
- 发布 APK、Windows x64 EXE 和 SHA256SUMS.txt；内部版本沿用 Android 0.2.3（14）和 Windows 0.1.6，Windows EXE 未代码签名。发布不扩大已验证范围，详见 [构建交接](docs/release-handoff-2026-09-21.md)。

## 维护阶段记录（v0.2.5 及之前，按主题归档）

以下是累计维护记录，不表示每一项都首次出现在 v0.2.5；本次发布增量见上节。

### Closure baseline (2026-09-15)

- StudyJournal 进入功能冻结与维护阶段；本仓库继续保留现有品牌、包名、应用标识、签名兼容关系和数据路径。后续产品演进将在独立的 Exocortex 仓库中进行。
- 完成 AI 学习助教的学习效果修复批次 P0-P4：补充客观判题指标与证据门槛、让可用效果证据回流规划、修复投影状态优先级、隔离 JSON 角色提示词、增加答案泄露检查，并保持正式事实、同步与 UI 主口径边界不变。
- 修复备份导出中的缺失资源引用和跨日志/模板/知识播客的孤儿资源判定；恢复入口继续保持严格完整性校验。
- 语音资料上下文改为按模型上下文窗口预算，并避免结构化正文转纯文本时重复公式内容。
- 修复本地日期范围、DeepSeek 默认模型与旧模型迁移、原生壳预览隔离、草稿保存错误可诊断性，并移除确认未使用的依赖和样式。
- 修复多设备离线创建同名标签后，云快照或普通/流式恢复因唯一索引冲突而整体失败的问题；恢复时按确定性规则保留同名标签，使设备收敛到同一结果。
- 修复 React StrictMode 并发触发两次存储初始化时，默认标签竞态写入导致未处理 `ConstraintError` 的问题；同一时刻的初始化现在合并执行，完成或失败后仍可再次运行。
- 修复语音复述打开键盘输入时反而关闭字幕、导致本轮回复完成后从界面消失的问题；打开输入会保证字幕可见，主动收起输入时才同步关闭。
- 冻结范围、自动化验收、数据边界及仍开放的真机/真实服务门禁见 `docs/studyjournal-final-freeze-2026-09-15.md`。

### Added

- 语音复述增加独立的百炼 Qwen Audio 3.1 TTS 预置，并为 3.1/3.0 配置各自适用音色；两款百炼和豆包 TTS 2.0 完成受控 Android 真实 LLM 回复播放、缓存重播及清理验收，不扩大到 ASR 或账单验收。

- 语音复述新增阿里云智能语音交互 NLS 识音石 V1 候选 ASR，使用独立的本机 AppKey + 临时 Access Token 凭据槽；协议测试与一次受控真实 WebSocket 生命周期验收已通过，Android 真机语音仍待验收。

- 语音复述支持在当前通话内点击助教回复重播；TTS 音频仅缓存于内存，显示占用并支持手动释放，通话结束后自动清理，不重复请求 TTS。
- 新增全局 OCR 失败看板、单条日志 Markdown/HTML/TXT 可读导出，以及 Desktop 原生编辑右键菜单；机器可读 JSON 传输包继续独立保留。

- 将“更多 → 使用教程”重构为九章任务导向产品指南，新增章节目录、快速开始、学习闭环、数据边界与按任务查找，并接入五张响应式流程插图；正式文案仅描述当前已实现能力。

- 语音复述的 ASR、LLM 与 TTS 可在开始页独立选择；豆包流式 ASR 1.0/2.0 接入 SAUC V3 与新旧两种鉴权，豆包 TTS 2.0 和旧版小模型 TTS 接入 Desktop/Android 宿主。端到端实时语音明确保留为独立链路预支持，不作为可混搭阶段；豆包 TTS 2.0 的短句真机播放于 2026-09-21 通过，其他产品和长会话/账单门槛仍未完成。

- 增加自动听说半双工候选模式：本地检测用户发言结束后自动完成 ASR、LLM 回复和 TTS 播放，并在播放结束后重新监听；保留手动输入模式，不提供全双工插话。
- 语音回复采用最多两段有限 TTS 预取，降低多句回复的分段等待；真实 Android 设备、网络和供应商延迟仍需单独验收。

- 语音复述接入真实 `ASR → LLM → TTS` 链路：桌面端由主进程托管 ASR WebSocket（`study-journal:voice-asr-*`），Android 端新增 `NativeVoiceAsrPlugin`（OkHttp）；播放改为 Web Audio 真实出声，回复按句流式合成。阿里云 Paraformer 实时识别、DeepSeek 回复、Fish Audio 合成已于 2026-09-09 用受控账号完成真实验证。
- 新增可选的线上验收测试：`aliyunAsrTransport.live.test.ts`（真实 ASR 会话）与 `voicePipeline.live.test.ts`（真实 LLM → TTS 回复链），未提供密钥时自动跳过，CI 保持确定性。
- 完成日志编辑器移动端布局收口：顶部保存栏、标签区域、工具栏和正文统一宽度基准，新增低对比“标签”输入标题，并将更多格式/插入工具改为紧凑分组工具带。
- 在间隔复习只读卡片上增加设备本地批注工具，支持笔迹、荧光笔、基础图形、文本输入和下拉选择；批注独立于正文、备份和云同步。
- 完成实时语音主动回忆阶段 0–6 自动化收口：localhost 隔离原型、协议无关 ASR/LLM/TTS 契约、schema 21 本机检查点、Web/Android PCM 采集、流式适配器、确定性 Mock 主链、复习内生产工作区、日志/当前卡片入口、本机摘要、Coach 确认转写，以及同步/备份隔离、模板升级和不可信学习内容防护。真实 Provider 与真机验收尚未包含在内。
- 重构语音复述原型视觉：开始页压缩标题、资料范围与输入模式占用；通话页采用全屏明亮声场、连续对话字幕、紧凑状态声纹和固定四键通话控制，保留可切换的深色声场，并覆盖 Desktop/Android 窄屏。

### Fixed

- 修复百炼 TTS 混用旧 text2audio 协议和音色导致的 HTTP 400；Android、Desktop、Web 按 Qwen Audio / 旧 Qwen-TTS 分流请求，签名音频升级为 HTTPS 下载，保留 WAV/MP3 格式，不放开 Android 明文网络限制。
- 优化 Android 学科弧形菜单的触达、文字对比和展开状态；仍只在确认具体学科后创建日志，不改变数据与导航语义。

- 修复语音复述三套实时 ASR 对终止帧依赖过强的问题：停止录音后保留已收到的 partial/final，终止帧丢失、延迟或连接先关闭时不再清空有效转写；豆包同步获得 1.5 秒完成宽限，自动重试也不再因运行代数变化而失效。ASR 配置页移除断句、语气词、数字规范化、多阈值等高级项，四个内置服务固定使用稳定默认参数并忽略旧覆盖值。

- 修复富文本编辑器复制公式到浏览器输入框时被过滤的问题：外部 `text/html` 载荷现在保留应用内所需的 `data-latex`，并为行内/块公式提供可见的 `$...$` / `$$...$$` 回退文本；公式选区边界与应用内结构化往返保持不变。
- 修复正文、公式与折叠块混合选区的应用内往返：应用生成的结构化剪贴板 HTML 现在优先于 Markdown 降级通道解析，Ctrl+X 也复用同一结构化载荷后再删除选区；跨应用仍提供可读的纯文本/HTML 降级。
- 修复日志编辑器移动端展开工具栏后的控件重叠：录音入口采用紧凑图标模式，状态/错误反馈独占整行，代码语言选择器在窄屏占用两个网格单元。

### Changed

- 语音默认模板升级为 `voice-default-cn@2`：ASR 由豆包切换为已验证的阿里云 Paraformer；豆包语音因测试应用未开通流式服务保留为备选。
- 语音工作区在缺少任一环节凭证、或在浏览器等不支持直连的平台时**拒绝开始通话**，不再呈现模拟通话；披露文案改为显示实际运行的模型。
- 教练解读与规划的模型调用禁用思考模式；确定性失败（密钥错误、Base URL 错误、跨域、结构不匹配）不再重试，避免按队列放大用量。
- AI/TTS 设置只有在密钥加载完成或用户实际编辑后才写入或清除，避免异步加载未完成时保存导致已存密钥被清空。
- 收紧今天、复习、日志和更多页面的顶部空间，并为复习进行态使用更紧凑的进度控制区。
- 批注工具栏固定在当前视口内，明确显示入口和当前工具的选中状态，并动态避让评分区。
- 数据库升级到 schema 21；完整恢复会清除临时语音会话/轮次并保留恢复前已有的本机通话历史，云同步和便携备份均不包含这些数据。
- 语音工作区返回来源 Tab 时保留原日志或未评分复习卡、队列和进度；语音练习不会自动评分或改写 FSRS。

### Fixed

- 修复 Android 打包依赖调用目录、重复执行 Web 构建与 Capacitor 同步，以及部分 PowerShell 环境缺少 `Get-FileHash` 导致 release 构建中断的问题；Debug/Release 现在共用绝对路径驱动的 assets 校验流程。
- 2026-09-09 二次审计 R1–R15：修复当前回答遗漏、多句转写覆盖、取消/静音/迟到授权、宿主帧类型与 ASR 截止时间、暂停恢复和 profile 凭据归属。
- 批注以持久化代次拒绝评分后迟到保存；教练 signal 贯通实际请求及原子提交；角色超时独立；用量区分已观测、估算与未知；主动保存历史改为稳定分页且不自动淘汰。
- 本次仅完成代码与安装包候选冻结；修复前的单次真实服务结果不代表冻结包已验收。详见 `docs/second-audit-repair-acceptance.md` 与 `docs/archive/release-freeze-2026-09-09.md`。

- 修复移动端编辑器“更多操作”菜单被顶栏裁剪导致完全不可点击的回归。
- 修复复习卡片切换时批注草稿未重置、导致旧笔迹渲染到新卡且新笔迹写入上一张卡的问题。
- 修复 AI 问答缺少超时与取消导致输入框永久禁用；桌面 TTS 与 Android AI 取消会中止本机请求；供应商已产生的消耗仍须以账单核对。
- 修复浏览器返回在 AI 导出、模板、TTS 设置与播客模板四个路由被吞掉的问题。
- 修复播客预览种子覆盖开发者真实 AI 密钥的问题。
- 修复评分后切换导航 Tab 会丢失撤回能力的问题；撤回栈现在在 App 未退出时保持可用。
- 评分成功后清除对应批注草稿，撤回评分不会恢复已经清除的批注。

## [0.2.3] - 2026-09-07

首个公开源码快照。

### Added

- 本地优先的学习日志、富文本和结构化内容编辑。
- 轻回看与 FSRS 记忆卡复习流程。
- 基于决策块的 AI 学习助教、自适应训练与延迟验证。
- 可选 Google 登录与 Firebase 增量云同步。
- 完整 ZIP 备份、Android 增量文件夹备份、OCR、录音和知识播客。
- “温润阅读”和“清爽现代”双视觉主题，以及独立浅色/深色模式。

### Reliability

- schema 19 迁移、同步墓碑、内容冲突归档和隐私导出边界。
- 大日志库分页/增量呈现、移动端编辑工具分组、系统返回和遮挡回归保护。
- 119 个 Vitest 文件（796 项测试）、30 个跨视口 Playwright 场景和 3 个 Firebase Emulator 场景。

### Packages

- Android：`versionName 0.2.3`，`versionCode 14`。
- Windows：`0.1.6`。

### Known release gates

- 真实 Android 设备上的中文 IME、系统返回、前后台和覆盖升级仍需逐设备人工验收。
- 真实 Firebase 账号的 reads/writes、Storage 和账单指标仍需受控验收。
- Windows 安装包尚未使用商业 Authenticode 证书。
