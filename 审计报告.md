# 408 StudyJournal 全面审计报告

> 审计日期：2026-09-09
> 审计对象：仓库 `https://github.com/a1234tuan/StudyJournal.git`，分支 `main`，HEAD `5f3cdc0`
> 审计范围：最近一次重构（实时语音主动回忆阶段 0–6、复习批注与评分撤回、记录编辑器移动端布局收口）之后的全面回归审计
> 审计方法：源码静态审查 + 自动化用例回归 + 关键断言交叉验证

---

## 0. 概述

### 0.1 最近改动概览（本次重构触及的模块）

| 提交 | 主题 | 主要改动模块 |
|---|---|---|
| `5f3cdc0` | 编辑器布局收口 + 文档 | `RecordEditorPage`、`styles/*`、`visual-v2.css`、`VoiceRecallPresentation`（生产通话 UI）、`ReviewAnnotationSurface` |
| `f48fd61` | 保存工作区状态 | `VoiceRecallPrototypeApp`（原型） |
| `3c157fe` | 实时语音主动回忆工作流 | 新增整棵 `src/features/voiceRecall/**`（27 个源文件）、Android 原生 `NativeVoiceCapturePlugin`、`VoiceCaptureController`、桌面 `preload/main`、`tabNavigation`、`webNavigationHistory`、`App.tsx` 路由集成 |
| `32f4404` | 复习批注 + 评分撤回 | `src/features/reviewAnnotations/**`、`reviewSession/runtime`、`ReviewPage`、`pages.css`、`storageAdapter` |

### 0.2 自动化回归基线（本机实测）

| 项 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `npm run test` | **126 文件 / 840 测试全部通过**（39.3s） |
| 类型检查 + 生产构建 | `npm run build`（含 `tsc -b`） | **通过**，产物正常输出 |
| Playwright e2e | `npm run test:e2e` | 未在本机运行（需浏览器二进制；项目基线声称 42 场景） |
| Firebase Emulator | `npm run test:firebase` | 未在本机运行（需 emulator；项目基线声称 4 场景） |

> 说明：自动化用例全绿，说明重构没有破坏既有确定性逻辑。但**自动化用例只覆盖了已接线的部分**——语音管线在测试里以 Mock Provider 形式存在，而生产路径根本未接入该管线（见 §3.1 的 CRITICAL 级发现）。这是本次审计最重要的结论。

### 0.3 审计结论摘要

共发现 **23** 项问题，按严重度分布：

| 严重度 | 数量 | 代表问题 |
|---|---|---|
| Critical | 2 | 语音真实管线未接入生产；ASR/教师回复为硬编码假字符串 |
| High | 6 | 无用量计费/限额；重试无退避且重试不可重试错误；通话区无滚动裁切；批注工具栏尾控件不可见；AI 驾驶舱信息面板为空壳 |
| Medium | 11 | 浏览器历史 4 个子路由无法恢复；侧栏双高亮；评分栏悬空；通话字幕重复；过渡态提交抛错等 |
| Low | 4 | 主题 token 缺失降级、桌面宽屏错位、超长 chunk 等性能提示 |

**最优先三项**：
1. 语音主动回忆的生产接线（CRITICAL，见 §3.1）。
2. 计费与熔断兜底（HIGH，见 §2.B）——这是“成本飙升却无法发觉”的直接风险点。
3. `aiClientService` 默认无超时 + orchestrator 无退避重试（HIGH，见 §2.A5/B2/B4）。

---

## 1. 基础交互与路由

### 路由模型（便于阅读后续结论）

- `TabKey = "today" | "journal" | "categories" | "review" | "more"`（`src/lib/tabNavigation.ts:3`）。
- `more` Tab 通过 `MoreSubRoute` 联合类型承载子路由（`tabNavigation.ts:4-20`，共 14 个子路由 + `null`）。
- 全局状态是一个 `TabMemory` 大对象，统一经 `commitNavigation`/`updateNavigationState` 修改（`App.tsx:202, 237-279`）。
- 返回有三条路径：Web/Desktop 的全局“返回”按钮（`App.tsx:1542-1550`）、各排除子路由自带的 `onBack`、原生 `CapacitorApp.backButton` 监听（`App.tsx:650-692`）。

### 1.(a) 页面跳转逻辑

**[A-1, Medium] 浏览器历史无法恢复 4 个子路由，前进/后退会脱节**
- 文件：`src/lib/webNavigationHistory.ts:39-52`
- 现象：`MORE_SUB_ROUTES` 校验数组缺少 `aiExport`、`templates`、`ttsSettings`、`podcastTemplates`，而这四个都是合法的 `MoreSubRoute` 值（`tabNavigation.ts:4-20`）。`restoreWebNavigationSnapshot`（`:240-242`）会把这四个子路由恢复为 `undefined` 并返回 `null`，`popstate` 处理器随之提前退出（`App.tsx:568-570`）。
- 失败场景：Web 端从 更多 → 知识播客 → TTS 设置，按浏览器“后退”（回到 `podcasts`，正常），再按“前进”试图回到 TTS 设置——快照无法恢复，视图卡在播客页而浏览器指针已前移，随后任何应用内导航都会把错位的历史条目推入栈。`templates`/`podcastTemplates`/`aiExport` 同理。应用内“返回”按钮仍可用（直接调 `popCurrentTabDepth`），故非硬死路，但浏览器 chrome 导航对这四屏是坏的。

**[A-2, 验证通过] 无非法路由键**：`App.tsx` 中 24 处 `openMoreSubRoute(...)` 与 5 处 `switchTab(...)` 均使用类型内合法键，无指向不存在路由的调用。

### 1.(b) 交互逻辑是否符合直觉

**[B-1, Medium] 硬件返回与应用内返回把自适应复习任务导向不同 Tab**
- 文件：`AdaptiveReviewPage.tsx:156`（`onBack={closeAdaptiveTask}`）→ `App.tsx:439-446`；硬件返回分支 `App.tsx:650-658` → `popCurrentTabDepth` → `tabNavigation.ts:308-311`
- 现象：应用内返回把 `activeTab` 设回 `"review"` 并清 `today.adaptiveTaskId`（回到复习 Tab，符合用户从复习教练打开的预期）；而 Android 硬件返回走 `popTabDepth("today")`，只清 `adaptiveTaskId`、**保留** `activeTab="today"`，把用户扔到“今天”仪表盘而非复习 Tab。
- 失败场景：从复习教练打开自适应任务、按系统返回键的用户，预期回到复习页，却落到今天页，需要重新导航。

### 1.(c) 按钮跳转是否正常

**[C-1, 验证通过]** 未发现指向错误/缺失/陈旧路由的按钮。`MorePage.tsx:79-137` 的列表行全部正确映射到 `App.tsx:1162-1176` 的处理器；侧栏 `navItems`/`bottomNavItems` 派发正确；收藏与全局创建动作解析到合法 Tab。

### 1.(d) 语义冲突

**[D-1, Medium] `more/recordings` 下侧栏同时高亮“录音”与“更多”两项**
- 文件：`App.tsx:174-180`（`navItems` 同时含 `{tab:"more", subRoute:"recordings", label:"录音"}` 与 `{tab:"more", label:"更多"}`）；激活判定 `App.tsx:1571-1573`
- 现象：激活判定为 `item.subRoute ? (activeTab==="more" && subRoute===item.subRoute) : (activeTab===item.tab)`。在 `more/recordings` 时，“录音”走第一支为真、“更多”走第二支 `activeTab==="more"` 亦为真，两项同时 `className="active"`。
- 失败场景：用户无法从侧栏判断当前身处哪个分区。

**[D-2, Low/Medium] 自适应复习任务渲染在“今天”Tab 下，却由“复习”发起**
- 文件：`App.tsx:429-437`（`openAdaptiveTask` 设 `activeTab:"today"`）、`App.tsx:1184-1199`（`today` 分支渲染 `AdaptiveReviewPage`）；发起点 `App.tsx:1510` `onOpenAdaptiveTask`
- 失败场景：在复习页点击自适应任务，用户被瞬移到“今天”Tab（底栏高亮“今天”而非“复习”），对一个复习发起的活动来说语义错位；`immersiveTaskActive`（`App.tsx:1527-1532`）也按“今天”沉浸任务处理。

**[D-3, Low] 复习会话退出按钮文案“返回复习”实际进入“卡片库”**
- 文件：`ReviewPage.tsx:966-973`
- 现象：会话态退出按钮文案“返回复习”带左箭头，但 `onClick` 调 `onModeChange("manage")`，切到卡片库/管理视图（`ReviewPage.tsx:925`）而非复习队列/概览。会话中模式 Tab 被隐藏（`:918`），这是唯一可见出口，文案承诺“返回复习”却打开卡片管理。

**[D-4, Low] AiExportPage 按钮文案“展开后选择格式并导出”实际只展开**
- 文件：`AiExportPage.tsx:106-108`，`onClick` 仅 `setExportOpen(true)`；真正导出是展开后另一按钮（`:100-103`）。文案含“并导出”但动作不含导出。

### 1.(e) 死界面 / 路由回不去

**[E-1, 验证通过] 无真正死界面**。`TtsSettingsPage`、`PodcastTemplatesPage`、`FavoritesPage`、`SettingsPage`、`OcrSettingsPage`、`BackupPage`、`StatsPage`、`TrashPage`、`UsageGuidePage`、`TemplateLibraryPage` 列表视图均无内嵌返回按钮，但：
- Web/Desktop：这些子路由都不在 `showWebNavigationBack` 排除清单（`App.tsx:1550` 仅排除 `recordings/ai/podcasts/aiTools/aiExport`），`getTabDepth` 为 1（`tabNavigation.ts:252-253`），故全局“返回”按钮可见。
- 原生：硬件返回在 `getTabDepth>0` 时走 `popCurrentTabDepth`，经 `popTabDepth` 回到 More 根（`tabNavigation.ts:378-436`）。
- 被排除的 5 个子路由也都各自接了 `onBack`/`aiWorkspaceOnBack`（`App.tsx` 914/1067/1073/1085/1124/1148/1013），无死路。

唯一“卡顿”是 §1.(a) 的浏览器历史脱节，但应用内返回始终可用。

---

## 2. 后端请求与成本控制

### 前置事实（关键框架）

语音流式适配器（`OpenAiCompatibleLlmStreamAdapter`、`FishAudioTtsStreamAdapter`、`TransportAsrStreamAdapter`、`VoiceRecallPipeline`）**已实现但未接入任何生产请求路径**（见 §3.1）。`createVoiceAsrAdapter` 因“需要已验证的宿主或中继传输”会抛错（`providerFactory.ts:17`），且生产根本不调用它。因此，本节的成本/熔断风险分两类：
- **已生效**：复习教练（`reviewCoach`）、知识播客（`knowledgePodcast*`）——这两条路径确实发起了真实付费 API 调用，是当前**唯一**的成本风险源。
- **潜伏**：语音管线一旦接线，A1/A2/A3/B7 等缺陷立刻生效。

### 2.(a) 容错与熔断

**[A1, High] 语音重试/熔断原语是死代码，从未接到任何适配器**
- 文件：`providerRuntime.ts:62-87`（`VoiceProviderCircuitBreaker`）、`providerRuntime.ts:133-175`（`retryVoiceProviderStream`）
- 现象：`pipeline.ts:46,70,84` 直接调用 `this.asr.transcribe / tts.synthesize / llm.complete`，未包重试或熔断；三个具体适配器（`openAiLlmStreamAdapter.ts:40-85`、`fishAudioTtsStreamAdapter.ts:18-74`、`transportAsrAdapter.ts:30-57`）均单次尝试。`retryVoiceProviderStream` 仅在 `providers.test.ts:170` 引用；`VoiceProviderError.retryable` 被适配器置位却**无任何读取方**。
- 失败场景：管线一旦接线，LLM 偶发 502/429 或 ASR 丢一帧就整轮失败，无重试无熔断；基础设施已写好却零防护。

**[A2, Medium] LLM 流式：20s 硬超时、无重试、无熔断（且会中途打断流）**
- 文件：`openAiLlmStreamAdapter.ts:36,41`
- 现象：`createTimeoutSignal` 是 `setTimeout` 壁钟超时，会在 20s 处**不顾流式进度**地中断。429 立即抛错无退避。`stream_options.include_usage` 已发但 usage 仅作为事件发出、未做强约束。

**[A3, Medium] Fish Audio TTS 首块后无超时**
- 文件：`fishAudioTtsStreamAdapter.ts:19,58-60`
- 现象：收到首块即 `timeout.dispose()`，随后读取循环**无超时**，仅靠外部 `signal` 中止。Provider 发首块后挂起 → 轮次挂起至用户手动取消，音频焦点不释放、后续轮次无限排队。

**[A4, Medium] ASR transport 无超时无重试，可无限阻塞**
- 文件：`transportAsrAdapter.ts:30-57`：`for await (const event of session.events) yield event;` 无超时包裹，`finally` 里的 `await session.close()` 在生成器永不返回时不会执行。

**[A5, High] `aiClientService` 无重试无退避无熔断；默认 `timeoutMs=0`（即无超时）**
- 文件：`aiClientService.ts:331-336`：`const timeoutMs = options.timeoutMs ?? 0;` —— 调用方不传则 fetch **完全无超时**。非 2xx 抛普通 `Error`、无 `retryable` 分类（`:362-370`）。复习教练各 gateway 均把 `timeoutMs` 透传为可选（`sessionPlanningGateway.ts:61`、`quizExecutionGateway.ts:35`、`feedbackInterpretationGateway.ts:63`）。
- 失败场景：Provider 429 限流 → 单次抛错无退避；或 TCP 连接在响应中途挂起 → `response.text()` 永不 resolve，无超时，orchestrator 重试循环卡在第 1 次直到永久，整条反馈队列被一个挂起请求冻结。

**[A6, Medium] SSE 解析器无缓冲上限**
- 文件：`sse.ts:7-25`：`buffer += decoder.decode(...)` 无大小封顶。Provider 或代理注入无边界的 keep-alive 会让 tab 内存增长直到 OOM。

**[A7, Low] TTS 链失败导致整轮失败，无优雅降级**
- 文件：`pipeline.ts:67-101`：`ttsTail.then(...)` 任一句失败即向调用方抛错，已产出/已播放的 token 与音频无法挽回，ASR/LLM 成本沉没、整轮标记失败。

### 2.(b) 成本控制

**[B1, High] 全链路无用量计费、无配额、无封顶、无告警**
- 证据：grep `costSpend|quota|spendLimit|usageCap|dailyLimit|rateLimit|logUsage|aggregateUsage` 在任何 AI 路径**零命中**。
- `VoiceUsageMeter`（`providerRuntime.ts:112-124`）逐 `runTurn` 构造、从不持久化、从不与任何预算对比、从不外显。`parseUsage`（`aiClientService.ts:264-281`）返回 token，orchestrator 把 `promptTokens/completionTokens/totalTokens` 存到批次/解读记录（`orchestrator.ts:322-326,498-502`），但**无任何汇总、封顶、告警或阻断逻辑**。
- 失败场景：用户（或一个 bug）触发数百次反馈解读/测验轮次，每次都计费，没有任何计数器中止；用户只能在 Provider 账单上发现。这正是题目所担心的“成本飙升而无法发觉”。

**[B2, High] 复习教练 orchestrator 对所有错误无退避重试，含不可重试错误**
- 文件：`orchestrator.ts:290-344`（反馈解读，默认重试 2 次、最多 3 次调用）、`orchestrator.ts:440-522`（会话规划，默认 1 次、最多 2 次）
- 现象：catch 仅特例 `AbortError`，**其余所有错误都重试**。因 `aiClientService` 抛普通 `Error` 无 `retryable` 标志，无法区分 `401 密钥无效`/`400 坏请求`/`JSON 解析错` 与瞬态 `502`。**重试间无延时**（`for` 循环立即再调）。
- 失败场景：用户配错 API Key → 每条反馈解读产生 3 次完整 LLM 调用（解析失败那次模型已产出 token，每次重试都是全新全量计费）后才标 `failed`；50 条反馈 = 150 次计费调用。

**[B3, Medium] `planSession` 对 `insufficient-context`（“我帮不了”）也重试**
- 文件：`orchestrator.ts:484` 在重试 try 块内 `throw`，catch（`:509`）再循环；对比 `feedbackInterpretation`（`:328-331`）把 `insufficient-context` 当终态成功处理。
- 失败场景：模型正确回答“背景不足”的那批，被用**最大**载荷（`contextWindowTokens - 8k`，`analysisPlanner.ts:45-46`）再请求一次，2 倍最大上下文计费。

**[B4, High] 挂起 LLM 请求 + 重试循环 = 静默冻结，无成本上限**
- 文件：`aiClientService.ts:331-336` 默认超时 0（A5）+ orchestrator 重试 `for` 循环（`orchestrator.ts:309,454`）`await`
- 失败场景：一次无 `timeoutMs` 的 `interpretFeedback` 调用挂起 → 反馈队列 worker（`feedbackInterpretationWorker.ts:31-38`）在该任务上永久阻塞，无告警（无人监听）。

**[B5, Medium] 知识播客 TTS 无单次/单元超时，parts 循环无封顶**
- 文件：`knowledgePodcastJobService.ts:494-514` 遍历 `parts` 调 `provider.synthesize(part, {signal})` 无单次超时；`FishAudioTtsProvider.synthesize`（`knowledgePodcastService.ts:591-631`）对 429/5xx 有 3 次退避（好），但**任何单次 fetch 无超时**；`AliyunTtsProvider`（`:634+`）**完全无重试**。
- 失败场景：长播客 × 多单元 × 多 part，某 part 的 fetch 黑洞 → 整单元及后续单元永不生成、任务永远“生成中”，无字符总量封顶。

**[B6, Low] 播客脚本解析失败重试 = 2 倍计费（可接受）**
- 文件：`knowledgePodcastService.ts:525-574`：2 次尝试、5 分钟超时、输出有封顶（`scriptOutputTokens`）。解析错时整二次产出计费；网络错立即抛（好）。输入上下文除 context-window 预算外无硬字符封顶。

**[B7, Medium] 语音管线（接线后）无每轮/每会话预算**
- 文件：`pipeline.ts:32-103`：`runTurn` 无用量封顶，`VoiceUsageMeter` 仅观察。状态机允许 `maxSessionTurns=160`（`repository.ts:8`）但无 token/秒/字符预算。`TurnEndpointController`（`turnEndpointController.ts:7-11`）`longTurnWarningMs=45_000` 但 `onLongTurn` 仅回调**不自动停**。
- 失败场景（接线后）：半双工下麦克风常开，按 45s×160 轮 × (ASR 秒 + LLM token + TTS 字) 无聚合计量、无 kill-switch。

**[B8, Medium] 反馈解读队列无大小/成本封顶，重试叠加**
- 文件：`feedbackInterpretationWorker.ts:23-41`：处理任意 `jobs[]`，`maxConcurrency` 默认 1、上限不封顶；每任务内部重试 3 次（B2）。`activeJobs` 去重仅防并发重复，对成本无益。
- 失败场景：用户批量导入 200 条反馈 → 最多 600 次计费调用、无配额闸门。

**[B9, Low] `testAiProviderConnection` 无超时（但 16 token，成本可忽略）**
- 文件：`aiClientService.ts:528-551`：`maxTokens:16`、无 `timeoutMs` → 连接测试可对无响应端点永久挂起。成本可忽略，UX/韧性是真实问题。

### 2 成本/韧性总览矩阵

| 链路 | 重试 | 退避 | 超时 | 熔断 | 用量封顶 | 计费记账 |
|---|---|---|---|---|---|---|
| 语音 ASR（transport） | 无 | — | 无 | 已写未接(A1) | 无 | 仅逐轮、不持久 |
| 语音 LLM（OpenAI 兼容） | 无 | — | 20s 壁钟(A2) | 已写未接 | 无 | 仅事件 |
| 语音 TTS（Fish 流） | 无 | — | 仅首块(A3) | 已写未接 | 无 | 仅事件 |
| 复习教练 LLM（`aiClientService`） | client 无；orchestrator 2–3 次(B2) | **无** | 可选，默认 0(A5/B4) | 无 | 无 | token 存记录、从不聚合(B1) |
| 播客脚本 LLM | 解析错 2 次(B6) | 无 | 5min | 无 | 输出 token 封顶 | 仅诊断 |
| 播客 TTS（Fish） | 3 次带退避(B5) | 是(350ms×n) | 单次无 | 无 | 无 | 无 |
| 播客 TTS（Aliyun） | 无 | — | 无 | 无 | 无 | 无 |

---

## 3. 核心功能与模块验证

### 3.(a) 语音通话功能是否正常

**[3A-1, CRITICAL] 真实 ASR→LLM→TTS→播放管线从未接入生产**
- 文件：`VoiceRecallWorkspace.tsx:274-316`、`runtimeController.ts:11-30,65,121-127`、`pipeline.ts`（整文件）、`providerFactory.ts:10-32`
- 交叉验证（本审计独立 grep 确认）：
  - `runTurn` 仅定义于 `pipeline.ts:32`，**生产代码无任何调用点**（仅 `providers.test.ts` 引用）。
  - `createVoiceAsrAdapter/Llm/TtsAdapter` 定义于 `providerFactory.ts`，**生产无调用**。
  - `runtimeController` 用 `silentSink`（`runtimeController.ts:11`）和 `provider: {}`（`:65`），捕获到的 PCM 帧只计数（`:121-127`）后丢弃，从不送入 ASR 适配器。
- 失败场景：用户授权麦克风并“对话”，音频被采集即丢弃，不联系任何 Provider，无转写、无 LLM 回复、无 TTS 音频。功能在生产是一个 UI 外壳。
- 注：`AGENTS.md` 与 `docs/voice-recall-provider-configuration.md` 明确真实 Provider 为受控验收的发布门（`voice-default-cn@1` 等“候选模板”未经验证不可量产），故“生产不接真实 Provider”本身符合发布策略；但**生产工作区连已实现的 Mock Provider 管线都没接**（直接用硬编码字符串），这是超出“候选待验”范畴的接线缺口。

**[3A-2, CRITICAL] ASR“转写”与教师回复是硬编码假字符串，并写入持久化轮次/历史**
- 文件：`VoiceRecallWorkspace.tsx:56-62,76-81,258-262,288-302,340`（本审计独立 grep 确认行号）
- 证据：
  ```ts
  // :258-262 stopCapture
  const sample = route.topic?.trim()
    ? `我对"${route.topic.trim()}"的理解是：这里需要补充转写。`
    : "本机采集已完成；请在提交前检查并校对转写内容。";
  setMockAsrText(sample); setTranscript(sample);
  // :288,76-81 submitTurn
  const teacherText = createTeacherReply(confirmedText, turns.length); // 两分支固定模板
  // :298,302,340 持久化
  providerFinalText: mockAsrText || confirmedText, playedCharacterCount: 0,
  usage: emptyUsage,  // 永远全零
  ```
- 失败场景：保存的轮次与“本机通话历史”摘要记录了伪造的“ASR 文本”与伪造的教师回复；用户误以为有真实模型在评判。

**[3A-3, High] `PLAYBACK_FINISHED` 紧随 `LLM_REPLIED`，"speaking" 状态一闪即过**
- 文件：`VoiceRecallWorkspace.tsx:289-307`、`domain.ts:137-153`
- 现象：`LLM_REPLIED` 置 `speaking`/`systemCaptureGate=true`，紧接着同步 `PLAYBACK_FINISHED` 复原。无异步 TTS 播放间隔，“speaking”只存在一个同步 tick。
- 失败场景：声纹/门控 UI 永不进入“speaking”，`systemCaptureGate` 恒为假，“教师播放中，麦克风暂时关闭”提示（`VoiceRecallPresentation.tsx:307`）生产中永不出现。

**[3A-4, Medium] `INTERRUPT_AND_LISTEN` 不打断任何播放**
- 文件：`VoiceRecallWorkspace.tsx:400-404`、`domain.ts:154-162`、`playbackQueue.ts:27`
- 现象：打断分发只改状态机，不调 `playback.interrupt()`。生产用 `silentSink` 不可见；一旦接真实 sink，旧代音频会在用户“打断”后继续播完。
- 失败场景：用户点“打断并说话”，AI 语音盖过用户语音播完。

**[3A-5, Medium] `submitTurn` 不校验运行态，从 speaking/thinking/finalizing-asr 提交会抛过渡错**
- 文件：`VoiceRecallWorkspace.tsx:266-287`、`domain.ts:209`
- 现象：`ensureListening` 只处理 `paused`/`failed`，`SUBMIT_CAPTURE` 仅在 `listening && captureRequested` 合法。会话态确认区（`VoiceRecallPresentation.tsx:312`）在 `speaking`/`thinking` 仍渲染 → 点“确认并发送” → `transitionVoiceRecallState` 抛 `VoiceRecallTransitionError` → catch → 错误 toast，本轮丢失。

**[3A-6, Medium] 单例 runtime 在卸载时 `disposeView=pause()`，重挂后残留"paused"**
- 文件：`VoiceRecallWorkspace.tsx:176`、`runtimeController.ts:157-159`
- 现象：`voiceRecallRuntime` 是模块单例，跨挂载共享。卸载无条件 `pause()`；React StrictMode（dev）会双触发。恢复 effect（`:173`）只在 `activeSessionId !== route.sessionId` 时恢复，重挂不一定触发 → 通话静默掉到"paused"。
- 失败场景：通话屏因主题/键盘触发重渲而重挂，通话静默暂停且不自动恢复。

**[3A-7, Low] `VoicePlaybackQueue.interrupt` 重置 `tail` 但未 await 在途 `play()`**
- 文件：`playbackQueue.ts:27-34`：`this.tail = Promise.resolve()` 在旧 `sink.play()` resolve 前执行，若 `sink.stop()` 不同步拒绝在途 play，慢 play 可在新代窗口写入音频 → 打断后短暂重叠。

**[3A-8, Low] `startCapture`/`stopCapture` 用陈旧 React `capturing` 标志做守卫，双击可并发启动**
- 文件：`VoiceRecallWorkspace.tsx:228-249`：`if (capturing || busy) return;` 中 `capturing` 是同 tick 未更新的 React state，快速双击可两次通过守卫。`runtime.startCapture` 先 `await stopCapture()`（`runtimeController.ts:118`）部分缓解，但两个适配器可能短暂并存、首个 `MediaStream` 可能竞态。

### 3.(b) AI 驾驶舱（通话信息显示）在最近改动后是否正常

**[3B-1, High] "AI 交互调用信息"面板无任何 AI/ASR/TTS 活动信息，仅静态标签**
- 文件：`VoiceRecallWorkspace.tsx:503`（生产 `vr-details` aside）
- 现象：生产面板只有 `资料 / 输入方式 / 状态` 三项静态字段 + 一句 Provider 提示。无转写、无 token、无时延、无代际、无 Provider 身份、无诊断。原型（`VoiceRecallPrototypeApp.tsx:298-310`）至少展示 `运行代际`、模板与 provider 选择器；生产面板是 stub。
- 失败场景：驾驶舱无法让用户核实哪条 Provider/模型处理了某轮，或 ASR/LLM/TTS 是否真的跑过。

**[3B-2, High] `elapsed` 是硬编码"进行中"而非计时器**
- 文件：`VoiceRecallWorkspace.tsx:480`：`elapsed={state.status === "paused" ? "已暂停" : "进行中"}`
- 现象：场景药丸渲染 `{elapsed} · {selectedModeLabel}`（`VoiceRecallPresentation.tsx:296`），全程静态字符串。通话时长永不前进。

**[3B-3, Medium] 持久化 usage 恒为 `emptyUsage` 全零**
- 文件：`VoiceRecallWorkspace.tsx:56-62,333-342`（已确认）：所有历史记录 `usage` 全零。若驾驶舱/历史展示用量估算，永远读 0。

**[3B-4, Medium] "字幕输入"键耦合两个独立状态且 `aria-pressed` 错标**
- 文件：`VoiceRecallPresentation.tsx:321`：单按钮同时 `onToggleCaptions()` + `onToggleTranscriptEditor()`，`aria-pressed` 只反映 `transcriptEditorOpen`。原型（`:349`）是两独立控件、`aria-pressed={captionsVisible}`。
- 失败场景：读屏用户得到错误 pressed 态；用户无法在不打开编辑器的情况下单独关闭字幕。

**[3B-5, Medium] 标题（教师台词）是上一轮的 canned 回复，非实时 LLM 流**
- 文件：`VoiceRecallWorkspace.tsx:482`、`VoiceRecallPresentation.tsx:304`
- 现象：`title={turns.at(-1)?.teacherText ?? "..."}`；`thinking`/`speaking` 期间标题冻结在上一轮，无"正在生成…"或流式片段（生产无流式）。

**[3B-6, Low] `vr-details` 固定定位浮于对话之上、无遮罩无 Esc**
- 文件：`VoiceRecallWorkspace.tsx:503`、`voiceRecallPresentation.css:562-574`：`position:fixed; top:66px; right:16px; z-index:8` 无 backdrop、无 Esc 处理。窄屏 340px 面板覆盖对话/标题；仅 X 按钮可关，点外部不关。

**[3B-7, Low] 驾驶舱控件死 CSS**
- 文件：`voiceRecallPresentation.css:580-633`：`.vr-provider-section`/`.vr-call-palette-toggle`/`.vr-details > button` 等仅原型引用，生产 `vr-details` 都不用。非运行时 bug，但生产驾驶舱被裁剪后样式表未清理，易致后续维护踩坑。

### 3.(c) UI 界面与交互体验

> 该子项覆盖题目要求的 (1) 整体风格、(2) 通话界面、(3) AI 交互表现、(4) 首页与导航、(5) 编辑器与日志界面。其中 AI 交互的“文字是否正常显示/时延/回复过长/提示词合理性”与 §3.(b)、§2、§3.(c)-C 合并表述，避免重复。

#### (1) 整体风格一致性

**[C-1, Medium] 决策块解读卡回退到非主题靛蓝**
- 文件：`src/styles/pages.css:2671-2672`：`--accent-color` 未在 `theme.css`/`visual-v2.css` 定义 → 解析为字面回退 `#4f46e5`（靛紫）。规范调色板是暖 `#a45135`（`theme.css:13 --color-accent`）或现代绿 `#14735a`。
- 失败场景：复习教练反馈解读卡呈紫色条纹与淡紫底，与其余暖/绿主题屏严重违和。

**[C-2, Low] `--color-on-primary` 未定义；深色主题下白字浮浅绿低对比**
- 文件：`pages.css:3742,3771-3772,3835`：active 批注工具 chip / 复习库过滤徽章用 `color: var(--color-on-primary, #fff); background: var(--color-primary)`。深色主题 `--color-primary` 变 `#8bd5b7`（`theme.css:70`），白字浅绿低对比。

**[C-3, Low] 语音工作区遗留 CSS 引用未定义的旧 token（死 CSS）**
- 文件：`voiceRecallWorkspace.css:19,32,36`：`--accent`/`--accent-soft`/`--text-secondary` 等在 `theme.css` 未定义；`.voice-*` 选择器不被任何当前组件渲染（工作区委托给 `vr-*` 视图）。若未来重启用旧布局，颜色解析为 `invalid`、边框/背景整体丢失。

#### (2) 通话界面是否合理、沉浸

**[C-4, High] 通话对话区无滚动，长对话被裁切**
- 文件：`voiceRecallPresentation.css:313-331`（`.vr-call overflow:hidden`、grid `auto minmax(0,1fr) auto auto`）、`:403-410`（`.vr-conversation` 无 `overflow`/`overflow-y:auto`）；对话还带 `margin: clamp(36px,8vh,86px) auto 0`（`:425`）、助手轮次是 `clamp(1.2rem,2.4vw,1.65rem)` 的 `h1`（`:470-474`）。
- 失败场景：助手问题长 / 用户转写多行时，flex 列超出 1fr 行，对话底部（用户自己的话或问题结尾）被裁、无法滚动阅读——沉浸式通话核心诉求受损。

**[C-5, Medium] grid 行数错配：开字幕编辑器时 5 子项入 4 行模板**
- 文件：`voiceRecallPresentation.css:325-326`（4 行）vs `VoiceRecallPresentation.tsx:293-326`（5 个直接子项：header、conversation、transcript-confirm[条件]、turn-cue、controls）。
- 失败场景：通话中切"字幕输入"插入确认行，4 行模板耗尽，`vr-controls` 落入隐式 `auto` 行；当前仍可见但布局意图被破，后续行高收紧可能把控件推出屏。

**[C-6, Low] 宽屏居中对话与全宽 header/controls 错位**
- 文件：`voiceRecallPresentation.css:366-374`（header 全宽）、`:403-410`（对话横向 padding 硬封 ~760px）、`:500-508`（controls flex 居中）。
- 失败场景：1200px+ 桌面，场景药丸与对话文本不在同一左文本栅格，沉浸框参差。

#### (3) AI 交互表现（文字显示/时延/长度/提示词）

- 文字显示：§3.(b) B-1/B-5 已述——生产无实时流式文字，转写是硬编码假字符串、教师回复是 canned 模板；ASR/TTS 文字"显示"实质未发生。
- 时延：§2.A5/B4——`aiClientService` 默认无超时，理论上一个挂起请求会让整条队列无限等待，"时延过高"以"无限挂起"的极端形式存在。
- 回复长度：§3.(c)-C2/C3——管线对 LLM 输入无截断、无滑动窗，`max_tokens` 是唯一长度闸；`runtimeController.updateMemory` 累积 `coveredPoints/misconceptions/pendingTopics` 无封顶；无任何代码组装“会话记忆 + 源日志”的消息列表，若接线朴素，每轮会全量发所选日志（最多 10 条，`VoiceRecallWorkspace.tsx:451 maxSelectedRecords={10}`）+ 全部历史轮 → 输入 token 逐轮膨胀、最终溢出上下文窗口、输出可达 `max_tokens`。**潜伏**：仅接线后生效。
- 提示词合理性：

**[C-7, High] 生产代码中无任何语音教师 system/persona 提示词**
- 文件：`providerProfiles.ts`（仅 Provider endpoint/model profile，无 system prompt）、`domain.ts:74`（硬编码开场问题）、`VoiceRecallWorkspace.tsx:76-81`（`createTeacherReply` 两分支模板，与所选学习材料无关）
- 现象：无人设、无"一次一问"约束、无长度封顶、无对所选 `recordIds` 的接地。LLM 路径唯一类提示词串是 `openAiLlmStreamAdapter.ts:9-10` 的 `UNTRUSTED_LEARNING_CONTENT_GUARD`（安全守卫，合理但与教学行为正交）。
- 失败场景：即便接线真实 Provider，也无教学人格/约束，回复质量与长度完全不可控。

#### (4) 首页及导航栏 Tab

**[C-8, Medium] 底栏创建 FAB 扁平且矮于兄弟项，未抬升**
- 文件：`visual-v2.css:276,282` vs `src/preview/uiV2Prototype.css:689`
- 现象：生产 `transform:none !important`，不像原型 FAB 抬升 16px；48px 高也矮于 52px 兄弟 Tab。
- 失败场景：移动端（≤920px）创建键像夹在高 Tab 间的扁圆，非突出主操作，视觉失衡。

**[C-9, Low] 新记录面板桌面栅格在断点上方拥挤**
- 文件：`pages.css:39-45,68-75`，`@media(max-width:920px)` 才塌缩（`:1800-1803`）。
- 失败场景：~940px 平板窗口，创建键紧贴卡片边、subject-picker 列内塌，恰在断点上方才堆叠的临界点显得拥挤。

#### (5) 编辑器与日志界面

**[C-10, Medium] 评分栏呈"悬空"玻璃条**
- 文件：`pages.css:3422-3436`（`.review-bottom-controls position:sticky` 浮于底栏上方）、`visual-v2.css:316-317`（`.review-rating-bar` 去边框/圆角/阴影 + `backdrop-filter:blur`）
- 现象：bar `position:sticky` 浮于底栏之上，visual-v2 进一步去边框圆角阴影并加模糊，外层 `background:var(--color-bg)` 而内层半透明模糊表面——混合处理。非重复/折叠/重叠，但“未锚定/悬空”。
- 失败场景：长卡滚动时四个评分键浮在文上方无卡框，呈“悬空”观感。对应题目"评分栏是否悬空"——是。

**[C-11, High] 批注工具栏尾控件需隐藏式横向滚动，无可见 affordance**
- 文件：`pages.css:3776-3798`（`overflow-x:auto` 且 `::-webkit-scrollbar{display:none}`、`scrollbar-width:none`）；工具栏含 14 个工具按钮 + 色板 + 3 滑块 + 清除（`ReviewAnnotationSurface.tsx:304-323`）。
- 失败场景：600–900px 屏，色板/线宽/橡皮/不透明度/清除位于屏外、滚动条隐藏、无任何提示。对应题目"日志选择界面是否会出现不可滚动、无法选择某些按钮"——是（批注工具栏）。

**[C-12, Medium] 下拉选择批注的选项编辑器压住批注工具栏右半**
- 文件：`pages.css:3924`（select-editor `z-index:31; right:16px; bottom≥72px`）vs `:3776-3780`（toolbar `z-index:26`）。
- 失败场景：编辑下拉选项时，编辑器盖住工具栏右侧 redo/clear/色板，无法点。

**[C-13, Medium] 绘图模式下整内容面不可滚动**
- 文件：`pages.css:3875-3890`；`ReviewAnnotationSurface.tsx:325`（非 pointer 工具时 SVG 加 `drawing`）。
- 现象：`.drawing` 时 SVG `pointer-events:auto; touch-action:none` 覆盖整个批注根，吃掉所有指针/触摸，下方页面在切回"浏览"前无法滚动。
- 失败场景：选笔后想先下滚读日志再继续画，手势被吞为起笔、页面不滚。对应题目"不可滚动"——是。

**[C-14, Low] 编辑器 topbar/toolbar sticky 偏移硬耦合 58px**
- 文件：`visual-v2.css:579-596`（topbar `min-height:58px`）、`:657-660`（toolbar `top:58px`）。
- 失败场景：窄窗下保存指示或操作行换行两行，topbar 超 58px，toolbar 粘附处出现缝隙或重叠边框。

#### (3)-AI 交互补充：通话字幕重复显示

**[C-15, Medium] 开“字幕输入”时同一段转写渲染两次**
- 文件：`VoiceRecallPresentation.tsx:303`（对话内 `vr-user-turn` 静态 `p`）、`:312`（底部 `vr-transcript-confirm` textarea 绑定同一 `transcript`）、`:321`（单键同时切换两者）。
- 失败场景：点“字幕输入”，同一句以两处出现（上方只读灰文 + 下方可编辑），冗余且困惑。

**[C-16, Medium] 用户转写相对助手被弱化**
- 文件：`voiceRecallPresentation.css:427-433`（`.vr-user-turn` 小字灰）vs `:470-474`（助手 `h1` 大号全色 serif）。
- 失败场景：沉浸对话中用户自己的语音读作助手粗标题下的次级字幕，非对称、非对话感。

---

## 4. 其他发现（安全 / 性能）

**[X-1, 信息] 凭据存储设备本地，未硬编码**
- 扫描确认 API Key 经 `storage.getAiSecret/saveAiSecret` 设备本地存取（`AiSettingsPanel.tsx`、`TtsSettingsPanel.tsx`），不写入源码/日志/测试夹具/备份/云同步，与 `docs/voice-recall-privacy.md` 一致。
- `src/services/firebase.ts:10` 的 `AIzaSy...` 是 Firebase Web API Key，按 Firebase 设计本就公开（安全由 Auth + 规则保障），非凭据泄漏。

**[X-2, Low/性能] 主 bundle 偏大**
- `npm run build` 产物：`dist/assets/index-*.js` = **2.36 MB（gzip 671 kB）**，触发 Vite 500kB 告警。`charts`/`cytoscape`/`mermaid.core`/`editor` 等 chunk 均 > 430kB。建议动态 `import()` 拆分（mermaid/数学/图表/编辑器按需加载）以改善首屏。

**[X-3, Low/依赖] `npm audit` 报 75 漏洞（49 moderate / 24 high / 2 critical）**，多在 dev 依赖。建议 `npm audit` 复核、对可升级项做受控升级。

**[X-4, 信息] 项目测试基线扎实**：126 Vitest 文件 / 840 测试全绿，迁移、隐私导出、云同步隔离、语音 mock 链均有覆盖；但覆盖的是“已接线部分”，语音生产接线缺口（§3A-1）与真实 Provider 行为不在自动化覆盖内，与 `AGENTS.md` 所述“真实 Provider 与真机仍为发布门”一致。

---

## 5. 优先修复清单（按 ROI 排序）

| 优先 | 编号 | 问题 | 建议动作 |
|---|---|---|---|
| P0 | 3A-1/3A-2 | 语音真实管线未接生产、ASR/教师回复为硬编码假串 | 在 `runtimeController.createSession` 用 `providerFactory` 组装 ASR/LLM/TTS；用 `pipeline.runTurn` 驱动；移除 `createTeacherReply` 与 `sample` 假串，至少先接 Mock Provider 跑通端到端 |
| P0 | B1 | 全链路无用量计费/配额/告警 | 新增持久化用量表（已存的 token/秒/字符）+ 每会话/每日预算 + 超阈告警/阻断 |
| P0 | A5/B2/B4 | `aiClientService` 默认无超时 + orchestrator 无退避重试 + 挂起冻结 | 强制非零 `timeoutMs`；重试前按 `retryable` 分类、4xx/`insufficient-context`/解析错不重试；重试间指数退避 |
| P1 | 3A-3/3A-4 | speaking 一闪即过、打断不生效 | 接真实 sink 后按 `generation` 串音频与状态机；`INTERRUPT_AND_LISTEN` 调 `playback.interrupt()` |
| P1 | 3A-5/3A-6 | 非法态提交抛错、单例卸载即 pause | `submitTurn` 校验状态、禁用或排队；卸载区分 StrictMode 与真实卸载、按会话而非单例管理生命周期 |
| P1 | C-4 | 通话区无滚动裁切 | `.vr-conversation` 加 `overflow-y:auto` 或把对话放可滚动容器 |
| P1 | C-11 | 批注工具栏尾控件不可见 | 工具栏分页/换行或显式可滚动 + 可见滚动提示 |
| P1 | 3B-1/3B-2/C-7 | 驾驶舱信息空壳 + 无 persona 提示词 | `vr-details` 展示 Provider/模型/代际/token/时延；`providerProfiles` 增教学 system prompt（一次一问、长度封顶、接地所选日志） |
| P2 | A-1 | 浏览器历史 4 子路由无法恢复 | `webNavigationHistory.ts:39` 的 `MORE_SUB_ROUTES` 补 `aiExport/templates/ttsSettings/podcastTemplates` |
| P2 | D-1/B-1 | 侧栏双高亮、硬件返回落错 Tab | 激活判定对 `more` 根项排除 `activeTab==="more"` 的盲匹配；`popTabDepth("today")` 在 adaptiveTask 存在时回 review |
| P2 | C-1/C-2/C-3 | 主题 token 缺失降级 | 定义 `--accent-color`/`--color-on-primary`；清理 `voiceRecallWorkspace.css` 死 CSS |
| P2 | C-10/C-12/C-13/C-15 | 评分栏悬空、选择编辑器压工具栏、绘图态不可滚、字幕重复 | 评分栏加锚定卡框；select-editor 上移或加遮罩；绘图态保留指针滚动区或加"滚动模式"开关；字幕与编辑器解耦两键 |
| P3 | D-2/D-3/D-4 | 自适应任务落 today Tab、"返回复习"实进卡片库、AiExport 按钮文案 | 调整 `openAdaptiveTask`/渲染归属；按钮文案与动作对齐 |
| P3 | A1/A2/A3/B7 | 语音重试/熔断死代码、各适配器超时不全 | 接线时把 `retryVoiceProviderStream`+`CircuitBreaker` 包到三个适配器；TTS 读循环补超时；`runTurn` 加每轮预算 |
| P3 | X-2/X-3 | 主 bundle 过大、依赖漏洞 | 动态 import 拆 mermaid/数学/图表/编辑器；`npm audit` 复核升级 |

---

## 6. 附录：审计方法与验证

- **克隆**：`git clone` 到 `C:\Users\admin\Desktop\StudyJournal`，`main` 分支 HEAD `5f3cdc0`。
- **静态审查**：分 4 个维度（路由交互 / 后端成本韧性 / 语音与驾驶舱 / UI-UX）并行源码审查，每条结论均附 `file:line` 与代码片段。
- **关键断言交叉验证**：本审计对最严重的 CRITICAL/HIGH 结论（3A-1、3A-2、A-1、A5、B1/B2、C-4、C-11 等）均独立 `grep`/`Read` 复核源码行号确认，非仅采信子报告。
- **自动化回归**：`npm run test`（840 通过）+ `npm run build`（tsc 类型检查 + Vite 构建通过）本机实测。
- **未执行**：Playwright e2e 与 Firebase Emulator（需浏览器/emulator 二进制，未在本机跑；项目基线声称 42 + 4 场景）。

> 总体评价：重构工程质量高、测试基线扎实、隐私与数据隔离边界清晰；**主要风险集中在“语音主动回忆的生产接线缺口”与“计费/熔断兜底缺失”两处**——前者使核心新功能在生产实为外壳，后者是“API 成本飙升而无法发觉”的直接敞口。两者均属发布前应闭合的门。

---

## 7. 修复状态（2026-09-09 回归）

按 `glimmering-knitting-bachman.md` 计划完成 P0+P1+P2 修复。`npm run test` 851 通过（基线 840 + 新增 11），`npm run build` 通过。

### 已修复

| 编号 | 修复内容 |
|---|---|
| 3A-1 | `VoiceRecallWorkspace` 用 `providerFactory` mock 分支组装 `VoiceRecallPipeline`；`submitTurn` 调 `pipeline.runTurn`，PCM 帧经 `framesRef` 收集后送 ASR；音频经 `runtime.enqueueAudio` 入队 |
| 3A-2 | 删除 `createTeacherReply`/`emptyUsage`/`setMockAsrText`/`sample` 假串；转写与教师回复来自管线结果；`providerFinalText=result.transcript`、`teacherText=result.teacherText`、`playedCharacterCount=result.usage.ttsCharacters` |
| 3A-3 | `PLAYBACK_FINISHED` 改由 `runtime.awaitPlaybackDrained()` 完成后分发（带 generation 守卫），"speaking" 持续整段播放 |
| 3A-4 | `INTERRUPT_AND_LISTEN` 前调 `runtime.interruptPlayback()`；`CANCEL_TURN` 调 `runtime.cancelActiveTurn()` 中止在途 ASR/LLM/TTS 流 |
| 3A-5 | `submitTurn` 增加 `state.status==="listening"` 守卫，非 listening 态直接返回，避免 `VoiceRecallTransitionError` |
| 3A-6 | 重挂恢复 effect：`activeSessionId===route.sessionId && status==="paused"` 时自动 `RESUME`+`CONNECTED`，消除 StrictMode 残留 paused |
| 3A-8 | `capturing` 守卫改用 `capturingRef`（同步），消除双击并发启动竞态 |
| 3B-3 | `saveHistory` 用 `usageRef` 累计真值（`addUsage` 逐轮累加 `result.usage`），不再写全零 `emptyUsage` |
| 3B-4 / C-15 | 拆"字幕与键盘输入"为两控件（`aria-pressed` 各自正确），转写区仅在 `transcriptEditorOpen` 渲染，消除双重渲染 |
| 3B-5 | 标题/教师台词来自管线 `result.teacherText`，不再用 canned 模板 |
| A-1 | `webNavigationHistory.MORE_SUB_ROUTES` 补 `aiExport/templates/ttsSettings/podcastTemplates`，浏览器前进/后退可恢复 |
| B-1 | `App.tsx` 硬件返回在通用 depth 分支前插入 `today && adaptiveTaskId` → `closeAdaptiveTask()`，与应用内返回一致 |
| D-1 | 侧栏 active 收紧：`activeTab===item.tab && !(item.tab==="more" && subRoute!==null)`，消除 More 根双高亮 |
| C-1 | `pages.css` `var(--accent-color,#4f46e5)` → `var(--color-accent)` |
| C-4 | `.vr-conversation` 加 `overflow-y:auto; overflow-x:hidden; overscroll-behavior:contain` |
| C-5 | `.vr-call` grid-template-rows 改 5 行（`auto minmax(0,1fr) auto auto auto`） |
| C-10 | 评分栏重新锚定：恢复 `border/border-radius/background`，删 `backdrop-filter` |
| C-12 | select-editor `bottom` 抬到 `max(108px, calc(72px + env(safe-area-inset-bottom)))` 避开工具栏 |
| C-13 | 批注工具栏去隐藏滚动条、改 `flex-wrap:wrap; overflow-x:visible`；删除重复 `.review-annotation-svg.drawing` 规则（绘图态 `touch-action:none` 为绘笔必需，浏览工具为滚动出口，保留） |
| A5 | `aiClientService`：`AiRequestError{httpStatus,retryable,timedOut}`、`DEFAULT_AI_TIMEOUT_MS=60_000`（`?? 0`→`?? DEFAULT`）、`classifyHttpStatus`（408/429/5xx 可重试）、`isAbortError`/`isRetryableAiError` 导出 |
| B2 | `orchestrator` 两处重试循环：`isAbortError`→pending return；`!isRetryableAiError`→立即 break；`abortableDelay(500*2**attempt, signal)` 退避；`errorCode` 摘要化 |
| B3 | `analyzeFeedback` `insufficient-context` 由 `throw` 改为 `lastError`+`break` 终态，不再重试 |
| B9 | `testAiProviderConnection` 显式 `timeoutMs: 8_000` |

新增文件：`src/features/voiceRecall/webAudioPlaybackSink.ts`（真实 `VoicePlaybackSink`：AudioContext 解码 pcm-s16le、低 gain 0.15、`stop` 关闭在途源；AudioContext 不可用时静默回退）。生产单例 `voiceRecallRuntime` 注入此 sink，不再用 silent 单例。`runtimeController` 暴露 `beginTurnSignal`/`cancelActiveTurn`/`enqueueAudio`/`interruptPlayback`/`awaitPlaybackDrained`/`playbackGeneration`。

### 仍 deferred（发布门或本轮范围外）

- **真实 Provider 接线**（A1/A2/A3/A4/B7、C-7 教师 persona 提示词）：按 `AGENTS.md` 仍为受控验收发布门，本轮只接 Mock 管线跑通端到端，未触碰真实适配器 retry/熔断接线与 system prompt。
- **成本记账/配额/告警**（B1/B4/B5/B8）：按用户决策本轮只做韧性修复（超时/分类/退避/不重试），不引入 DB 用量表、配额或告警 UI。
- **驾驶舱信息面板扩展**（3B-1/3B-2 `elapsed` 计时器）：deferred。
- **Low/P3**：3A-7、3B-6/3B-7、C-2/C-3/C-6/C-8/C-9/C-14/C-16、D-2/D-3/D-4、X-2/X-3。

