# NoteProject Engineering Guide

## Current Baseline

- Public maintenance release v0.2.5 (2026-09-21) distributes the validated APK/EXE with SHA256SUMS.txt. The release tag is separate from Android 0.2.3/versionCode 14 and Windows 0.1.6; do not infer a platform version bump or broader live acceptance. README follows the in-app UsageGuidePage and links the publication evidence in docs/release-handoff-2026-09-21.md.

- The 2026-09-21 voice-recall ASR reliability follow-up fixes the reported Android stalls across DashScope Paraformer, NLS 识音石 V1, and Doubao streaming ASR. The first patch handled only a missing Paraformer `task-finished`; the completed follow-up also preserves recognized text when a provider sends only a partial result, loses/delays its terminal frame, or closes the WebSocket after capture ends, and applies the same 1.5-second recognized-text grace to Doubao instead of its former 30-second wait. No-text finalization now has a 12-second diagnostic timeout. The automatic retry captures the post-cancellation operation generation, so it actually reopens the microphone rather than invalidating itself. Built-in ASR endpoints/models/resource IDs and recognition options are fixed to verified defaults; the UI exposes only service selection and device-local credentials, ignoring stale legacy overrides. Safe provider failure detail is shown with tokens/long credential material redacted. Alibaba NLS remains the independently selectable `voice-asr-aliyun-nls-shiyinshi-v1` candidate with a separate AppKey + temporary Access Token slot; both its WebSocket lifecycle and a real paced 16 kHz Chinese PCM recognition producing a non-empty final transcript passed. Android physical-device speech, multi-turn, expiry, weak-network, concurrency and billing remain gates. See `docs/studyjournal-scope-unfreeze-2026-09-21-aliyun-nls-asr.md` and `docs/voice-recall-provider-configuration.md`.

- The 2026-09-21 review-coach presentation follow-up keeps answer evidence and source material readable without changing formal learning semantics: adaptive turns accept up to four image attachments, either pass them to a vision-capable provider or run local OCR before evaluation, and never persist the image bytes as a learning fact; AI Markdown code fences now have language labels, syntax highlighting, horizontal scrolling, and copy feedback; review-coach source previews reuse the read-only rich-editor renderer so formulas and structure nodes remain visible. Coverage is in `src/features/reviewCoach/AdaptiveReviewPage.test.tsx`, `src/components/AiMarkdown.test.tsx`, and `src/features/reviewCoach/ReviewCoachWorkbench.test.tsx`; the provider/backup/sync boundaries remain unchanged.
- The 2026-09-20 Android bottom-navigation create action is now an explicit subject-selection flow: tapping `+` opens a fixed five-slot radial subject picker with drag-to-snap browsing and a scrollable all-subjects fallback; only confirming a subject creates a record. It is presentational only, excludes archived subjects, closes on backdrop/Escape/Android Back, and does not alter record, sync, backup, or navigation semantics. See `src/components/SubjectRadialPicker.tsx` and `e2e/subject-radial-picker.spec.ts`.
- StudyJournal is feature-frozen for maintenance as of 2026-09-15, **except for the scoped daily-plan exception recorded in `docs/studyjournal-scope-unfreeze-2026-09-16.md`, whose scope was extended on 2026-09-17 by `docs/studyjournal-scope-unfreeze-2026-09-17.md`** (the "Daily Plan" feature: one new `dailyPlans` table at schema 24, an optional `RecordBlock.planId`, one new cloud-sync entity type, deterministic plan statistics, and - per the 2026-09-17 extension - the plan-metadata -> AI "intent vs fulfilment" feedback path, which is scoped but **not yet implemented and not yet designed**). Any other new feature still requires its own unfreeze decision. `docs/studyjournal-final-freeze-2026-09-15.md` is the canonical closure record. Keep this repository's StudyJournal branding, package/application identifiers, signing compatibility, and data paths stable. Future product development and all Exocortex renaming belong in a separate directory and repository; do not perform an in-place rename here - the daily-plan exception above is a scoped evolution inside the frozen baseline, not a reversal of that rule.
- **The 2026-09-17 scope decision moves the daily plan's AI feedback layer (L2) back into this repository.** On user instruction, `docs/studyjournal-scope-unfreeze-2026-09-17.md` extends the 2026-09-16 unfreeze to cover plan metadata entering the AI context plus versioned prompts that produce "intent vs fulfilment" feedback, and withdraws the earlier *functional-level* expectation that the daily plan feature would move to Exocortex. Two things are deliberately unchanged: the *repository-level* separation rule above (Exocortex still starts in a new directory and a new repository), and the product boundary in `docs/新的方案.md:738-750` - L2 model output stays advice text and may never decide task priority, formal mastery state, or delayed-verification dates, nor change any existing log/review/scoring/backup semantics. No L2 code and no L2 design exist yet; `docs/archive/exocortex-ai-cockpit-migration-and-enhancement-plan-2026-09-15.md` is marked paused (content untouched). Reading order for the deferred clauses: D1/D7 of `docs/daily-plan-final-plan-2026-09-16.md` and sections 5/7 of `docs/daily-plan-intent-2026-09-16.md` now carry inline 2026-09-17 revision notes; the superseded drafts (`docs/archive/daily-plan-detailed-design-2026-09-16.md`, `docs/archive/daily-plan-implementation-plan-v3.md`) are historical records and were deliberately left untouched.
- The 2026-09-17 today-page entry follow-up moves the daily-plan entry onto the title's own line, right-aligned to the header's edge, and restyles it from a bare `.link-button` into the shared `.primary-button` - the same class as the today page's 开始复习, so colour, text colour, font size, weight, radius, and height stay in sync by construction rather than by copied declarations. The entry sits 18px inside the header's right edge (`margin-right: 18px` on the title-actions slot, matching the header's own gap) so it does not read as a corner-stuck chip; set that to 0 to align it with the action group below. It is purely presentational and stays inside the daily-plan scope: no data, storage, sync, navigation, or copy changes. The shared `PageHeader` contract is deliberately left intact - the required layout is reached by re-laying only the today page's header as a two-column grid (`.today-page > .page-header`), which is what lets the title row span both columns; `PageHeader` only gained a `page-header-lead` class name on its lead column so that grid can address it, and `display: contents` on that column promotes its children into the grid. The motive for the scoped grid: `PageHeader` pins the action group to the bottom of the lead column (`align-items: flex-end`), so the goal pill, sync, and favourites sit on the *subtitle's* line and the title line has no reachable right edge - a plain `margin-left: auto` on the title-actions slot has no slack to travel into, which is exactly why the entry used to hug the heading. `docs/archive/daily-plan-ui-ux-review-2026-09-16.md` records the review this implements. Verified with a temporary browser probe (deleted after the run) at 1440x1000 (header `320..1360`; title row spans the full 1040px; entry `right=1342`, i.e. 18px in, on the title's line; action group still at `right=1360` on the subtitle's line) and 390x844 (title row spans the full 358px, entry `right=356`, action group on its own row); header height costs +9px desktop (88 -> 97) and 0px narrow (134 -> 134). `tsc -b` clean, `e2e/daily-plan.spec.ts` 16/16, and the full unit suite green (1435 passed / 3 skipped).
- The 2026-09-17 stats-fixture fix removes the last date-fragile unit case. `src/pages/StatsPage.test.tsx` used to pin `masteryTrend` to 2026-09-10/11 and expect "8 次有效回忆" twice; because `summarizeRecall` (`src/lib/learningStats.ts:30`) starts the 7-day window at `date - 6 days` and `StatsPage.tsx:41` feeds it the real `todayISO()`, the 09-10 row left the window on 2026-09-17, the 7-day card reported 4 instead of 8, and the case began failing on its own. The fixture's two sample rows are now derived with `addDaysISO(todayISO(), -2 / -1)`, so they stay inside every rolling window the page reads. Product behaviour was correct throughout - only the fixture was stale; any future stats fixture handed to `deriveLearningStats` must stay relative to `todayISO()` for the same reason.
- The 2026-09-17 daily-plan polish follow-up implements the four remaining items of `docs/archive/daily-plan-ui-ux-review-2026-09-16.md` (P2-01/P2-02/P2-03/P3-01), all presentational and inside the daily-plan scope. (1) The 更多 -> 应用 list now leads with a 今日计划 `ListRow` whose handler is the today page's own `openDailyPlan` - a cross-tab entry (precedent: 分类管理 reaches the categories tab), deliberately not a `more` sub-route, because the workspace lives in the today tab and a sub-route would render it twice. (2) The today/history switcher moved from `PageHeader.titleActions` onto `.daily-plan-topbar`, a back-left/controls-right row matching `.record-editor-topbar`; `titleActions` now has exactly one consumer (the today page's entry) and remains a documented `PageHeader` capability. (3) The subject share bar fills with `--color-primary` like every other share/progress bar; `--color-success` stays reserved for status. (4) The row delete control is a contained 36x36 icon button (`--radius-control`, vertically centred, 6px dead zone to the row) instead of a full-height `border-left` band, and gained `:focus-visible` parity; its accessible name is unchanged, so no test needed rewriting. One review claim was corrected along the way: the journal/review/daily-plan view switchers do *not* share one skin (the first two are underline tab bars, the daily-plan one is a segmented pill group) - only the anchor was unified, and the skin difference remains an open decision. Validated with a throwaway browser probe (geometry + computed styles, both viewports), `tsc -b`, the full unit suite (1436 passed / 3 skipped) and `e2e/daily-plan.spec.ts` 16/16.
- Known e2e flakiness (pre-existing, reproduced on 5afe1e4 before the polish commits): when four or more spec files run concurrently, `e2e/stats-page.spec.ts` intermittently times out at its default 30s while waiting for the 更多 nav button after `/?preview=stage3` (Vite cold transform plus 4 parallel workers). It passes alone or in smaller batches, on old and new code alike. Treat a stats-page timeout in a large batch as an environment artifact and re-run before investigating product code; a durable fix (raising its timeout like `daily-plan.spec.ts` does) is out of the current scope.
- The 2026-09-11 voice-recall stage is frozen after controlled Android physical-device validation of automatic half-duplex multi-turn conversation. See `docs/voice-recall-freeze-2026-09-11.md`. Manual input modes and end-of-call data actions remain open gates; keep live tests explicitly excluded from ordinary validation and require separately approved paid-provider tests.

- Canonical open-source branch: `main`. The pre-publication history remains in the legacy repository on `feature/review-effect-coach-v2`.
- The 2026-09-15 snapshot-restore convergence fix deduplicates same-name tags before both restore write paths. It chooses a deterministic winner (a live row before a deleted row, then the smallest id) so the `&name` unique index cannot abort cloud, ordinary, or streaming restore after two devices create the same tag name offline; the existing ledger tombstone flow reaps the loser. Coverage lives in `src/services/storageAdapter.duplicateTagCollision.test.ts`.
- The 2026-09-15 closure validation fixed two deterministic issues exposed by the full suite: `DexieStorageAdapter.initialize` coalesces concurrent React StrictMode startup calls while remaining rerunnable after completion or failure, preventing default-tag `&name` conflicts; opening voice keyboard input now keeps captions visible so the completed reply remains readable, while explicitly closing the input still hides both. Preserve both behaviors and their regression coverage.
- Product boundary: `docs/新的方案.md`.
- Database version: schema 24. Store definitions live in `src/db/reviewCoachSchema.ts` and migrations in `src/db/database.ts`; schema 19 finalizes confirmed legacy facts, schema 20 adds device-local review annotations, schema 21 adds the three local-only voice stores, and schema 24 adds daily plans. Voice/TTS maintenance does not add a schema version.
- AI cockpit implementation is complete: Stages 0-8 were completed and verified on 2026-09-07, and Stage 9 automated release acceptance is complete. Physical-device upgrade and controlled real-account Firebase quota sign-off remain release gates; do not describe the release itself as signed off until they pass.
- Product UI migration is complete through final automated acceptance. `src/styles/visual-v2.css` is the active visual layer; `reading` is the default visual theme and `modern` is the alternative. The visual theme is device-local and independent from the existing light/dark/system setting; do not add it to the database or cloud-sync contract.
- The UI migration preserves formal create/save/rating semantics and routes Review Coach through `Review -> Learning Coach`. All caught errors rendered by React pages/components must pass through `src/lib/uiError.ts`; `src/lib/uiErrorSurface.test.ts` prevents raw `error.message` regressions.
- The 2026-09-08 review-workspace follow-up adds compact primary-page headers, process-lifetime cross-tab rating undo, and a first-stage read-only annotation surface. The annotation toolbar is viewport-fixed and dynamically avoids visible rating controls.
- The voice-recall production workspace uses a real, independently selectable `ASR -> LLM -> TTS` pipeline with device-local credentials, real transcript/reply streaming, manual input modes, and automatic half-duplex mode. Automatic half-duplex has been validated on an Android physical device across multiple consecutive rounds using Aliyun ASR, the configured LLM, and Fish Audio TTS. Doubao streaming ASR 1.0/2.0 (SAUC V3), large-model TTS 2.0 (V3), and small-model TTS (legacy V1) now have protocol and Desktop/Android host support, with TTS 2.0 validated on Android on 2026-09-21; ASR and small-model TTS still need their own real-account acceptance. Doubao end-to-end realtime voice is a separate combined runtime and is only documented as pre-support, not exposed as a modular provider. SiliconFlow real-time ASR, barge-in, and PCM/Opus gapless playback remain unimplemented. Keyboard-confirmed LLM-to-TTS playback, cached replay, manual cache clearing, hangup cleanup and discard-return passed controlled Android tests on 2026-09-21 for Qwen Audio 3.1/3.0 and Doubao TTS 2.0. Microphone/manual recording, pause/resume, local-history/journal retention, weak networks and billing remain separate gates. `?preview=voice-recall` remains the isolated Mock call-control prototype. See `docs/voice-recall-tts-device-validation-2026-09-21.md`, `docs/doubao-voice-provider-compatibility.md`, `docs/voice-recall-freeze-2026-09-11.md`, and `docs/realtime-voice-recall-implementation.md`.
- The 2026-09-20 TTS replay follow-up keeps audio strictly session-local: each generated reply's chunks are cached only in memory for the active call, replay clicks reuse that cache without another TTS request, the live byte count and manual clear action are visible, and end/unmount clears it. No schema, disk, backup, cloud-sync, export, or formal learning fact changes. Saved voice history remains an explicitly retained device-local summary/usage record and is not AI context, sync, or backup data; see `docs/studyjournal-scope-unfreeze-2026-09-19.md` and `docs/voice-recall-privacy.md`.
- The 2026-09-09 record-editor follow-up is complete through automated acceptance: mobile editor chrome shares one width basis, the tag field is a low-contrast labelled input, and expanded formatting/insertion tools use a compact grouped toolbar. These changes are scoped to `.record-editor-page` and do not alter record persistence or editor document semantics.
- The 2026-09-12 editor toolbar follow-up is complete through automated acceptance: the log editor uses a compact icon-only recorder on narrow screens, recorder feedback occupies a full-width row, and the code-language selector spans two mobile grid cells. These changes remain scoped to `.record-editor-page` and do not alter record persistence or editor document semantics.
- The 2026-09-12 usage-guide follow-up is complete through automated acceptance: More -> Usage Guide is organized as a task-oriented document with nine chapters, six responsive semantic illustrations under `public/guide/`, and a narrow-screen horizontal chapter index. The guide describes only implemented product capabilities and keeps provider credentials, local voice history, backup, and sync boundaries explicit.
- The 2026-09-14 voice-context follow-up is complete through automated acceptance: voice material is now budgeted in tokens against the selected LLM profile's `contextWindowTokens` instead of a fixed 1200-char per-record / 6000-char total cap, `recordToPlainText` no longer duplicates formulas nested in collapse/highlight blocks (measured 10060 -> 8210 plain-text characters on the diagnostic record), truncation lands on node boundaries and is declared in the prompt, and the call-details panel derives "sent X / Y characters" on demand without persistence. `estimateAiTokens` lives in `src/lib/aiTokens.ts` and is re-exported by `aiContextService`. Physical-device verification of the new budgeting and the still-open knowledge-export markdown format change remain open gates; the frozen system prompt text and prompt version are unchanged.
- The 2026-09-14 backup-integrity follow-up closes audit findings F-01 and F-02. `prepareExportContent` now reconciles the exported copy in both export channels (`createSnapshot` / `createStreamableSnapshot`): a reference whose asset is not packed into the snapshot is dropped from the exported content, because export deliberately omits `generatedBy === "knowledge-podcast"` audio and historical data can also reference assets that no longer exist locally — either kind used to make `assertSnapshotIntegrity` reject the snapshot and permanently block every backup channel (manual zip, streaming export, folder backup, repository backup, cloud snapshot all share this code). Dropped references are counted separately (deliberately excluded vs. dangling) and logged with a `[backup]` prefix; the assertion still runs as a final guard and remains fully strict on restore, where the input is untrusted. Asset stripping lives in `stripRecordAssetRefs` (`src/lib/recordContent.ts`) and re-syncs `RecordBlock.assets` so a stripped node cannot be re-appended. F-02: `cleanupOrphanAssetsForRecord` now also scans `db.templates` (via `extractRecordRefsFromContent`) and podcast audio units before declaring an asset an orphan — it previously scanned only `db.blocks` and `db.recordDrafts`, so an image referenced by both a record and a template was irreversibly deleted and left the template dangling; `db.templates` and `db.knowledgePodcasts` are therefore part of the `permanentlyDeleteBlock` transaction. Coverage: `src/services/storageAdapter.assetRefs.test.ts` (11 tests, real Dexie via `fake-indexeddb`, verified to fail before the fix). Still open and deliberately not addressed here: restore still rejects a backup that references an asset its zip does not contain instead of degrading to a missing-asset placeholder the way `BackupPage` promises, and `zipToSnapshot` still ignores `payload.podcasts`.

- The 2026-09-14 A-tier audit follow-up closes the remaining P2/P3 findings the team elected to fix. Local-date correctness (F-03): the six `new Date().toISOString().slice(0, 10)` call sites in `AiKnowledgeScopePicker.tsx`, `KnowledgePodcastPage.tsx`, and `VoiceRecallWorkspace.tsx` now use `todayISO()` from `src/lib/date.ts`, because the UTC slice resolves to yesterday for the first eight hours of a China-local day and silently mis-scoped AI knowledge ranges; `src/lib/tencentSigning.ts` deliberately keeps its UTC slice because that is the Tencent Cloud signature date contract. Provider defaults (F-15/F-28): the built-in DeepSeek seed model is now `deepseek-v4-flash` (matching the AGENTS.md Stage 6 acceptance provider and the live `providerProfiles.ts` template) instead of the unverifiable `deepseek-v4-pro`, the legacy `deepseek-chat` migration maps to the same value, `createAiProviderTemplate` has a `default` branch so an unknown or legacy `builtIn` value read from persisted settings falls back to DeepSeek instead of returning `undefined`, and the model placeholder in `AiSettingsPanel` was corrected. Prototype-shell isolation (F-10): `?preview=ui-v2` and `?preview=voice-recall` now also require `!isNativePlatform() && !isDesktopPlatform()`, matching the stage3–stage7/coach/journal-performance shells, because both the Capacitor WebView and the Electron shell serve from a `localhost` host so the hostname check alone did not exclude them. Draft-save diagnosability (F-18): a failed draft save keeps the serialized queue alive but now records a `formatUiError(..., "record-save")` message in a dedicated `draftSaveError` state that is rendered and cleared on the next success, instead of only flipping the status pill. Dead-code cleanup: the unreferenced `recharts` and `idb-keyval` dependencies were removed from `package.json`, the lockfile, and the `charts` manual-chunks entry in `vite.config.ts` (`prosemirror-history` is retained — it is a real transitive dependency of `prosemirror-menu`); the two dead `.review-evaluation-panel/-toggle/-body/-state/-history` blocks in `src/styles/pages.css` were removed (149 lines, both the base block and a later override), keeping the live `.record-review-evaluation`, `.review-evaluation-inline-indicator`, and `.legacy-review-evaluation-history` rules. `runPaddleOcrViaBrowserFetch` in `src/services/ocrService.ts` stays as an unreferenced reference implementation with an explanatory comment and must not be deleted; wiring it up requires CORS, credential, and progress-contract validation first. Audit findings F-20 (the `reviewAnnotations/generation.test.ts` "orphan" test) and F-12 were verified to be false positives and were kept. The cross-cutting invariant #10 wording conflict was resolved by documenting in this file that the Firebase **web** config in `src/services/firebase.ts` is a public client identifier rather than a secret, while LLM/TTS keys remain fully covered by the no-secrets rule. Verified with a clean `tsc -b`, the full `163`-file / `1022`-test Vitest baseline, and a successful production `vite build`.

- The 2026-09-14 AI-cockpit learning-effect remediation (P0-P4) is complete through local acceptance; the paid Provider run is the only open gate. Batches executed in the reviewed order P0 (semantic contract, `docs/ai-cockpit-data-semantics-pre-stage-2026-09-14.md`) -> P1 (A) -> P2 (B) -> P3 (C) -> P4. Batch A made effects measurable without touching the UI: `InterventionEffectSummary` gained objective (AI-graded) fields (`objectiveAnswerCount/CorrectRate`, `verificationObjective*`, `objectiveEvidenceStatus`, `masteryAssessmentBreakdown`, `selfReportedMasteredCount` with the `immediateMasteredCount` alias kept) computed in `replay.ts`, `REVIEW_COACH_REPLAY_VERSION` is now `review-coach-replay-v3` so projections rebuild once, and `strategyKey` gained `actualPracticeType` + `hintLevelUsed` + the sorted set of turn-level prompt versions (F-14, option (i) of §2.5), so samples from different prompt regimes are never pooled. Batch B lets results flow back: B-1 replaced "last writer wins" projection status with explicit precedence (stale -> learning -> needs-analysis -> content events -> planned), B-2 hands only *objective* effect fields to `planSession` (gated on `objectiveEvidenceStatus`, capped at 5, scoped to the sub-batch's difficulty types, and folded into the frozen `inputFingerprint`) — never `retentionRate`/`selfReportedMasteredCount` (D-1 contract), and B-3 adds `listDecayedBlocksNeedingReplan` plus a workbench "需要重新规划" section whose note is recorded as ordinary **user-authored** feedback (no system-authored formal facts). Batch C cleaned the prompt surface: every structured-data call (`aiGateway`, `sessionPlanningGateway`, `quizExecutionGateway`, and the knowledge-podcast script, F-21) now sends a JSON-only role prompt (`rolePrompts.ts` / `PODCAST_SCRIPT_SYSTEM_PROMPT`) through the new `AiCompletionRequestOptions.systemPrompt`, which replaces `CHAT_SYSTEM_PROMPT` in memory only and can never reach the request body; `quizExecutionGateway` derives its `practiceType`/`answerMode` enums from the parser's own `quizPracticeTypes`/`answerModes` (three of the six old hard-coded values were invalid, F-04) and splits `requestedStrategy` from the practice type (F-07); `unreliable` assessments are recorded with an explicit `unreliable:` reason prefix and drive no strategy, difficulty or mastery change (F-06 — note `verificationPolicy.ts` still treats `unreliable` like `partial` for *scheduling only*, a registered known inconsistency); `questionIntegrity.ts` adds a deterministic local answer-leak check that runs before the paid quality review and counts as a quality failure (F-20), with `answer-leakage` added to `severeIssues`; and `validateBlueprintCandidates` now really gates cross-block feedback/interpretation/evidence references per mode (F-19). Verified with `tsc -b` clean, `npm run test` -> `175` files / `1108` tests passing with `3` opt-in live files skipped, and a clean production build. Open gates: the two-call paid acceptance (`REVIEW_COACH_LIVE_DEEPSEEK_KEY=... npx vitest run src/features/reviewCoach/aiAcceptance.live.test.ts`) and the known A-3 consequence that the workbench effect list can show more, smaller groups (top 3 by `strategyKey`, so a 0-sample group can appear beside a sampled one).

- The 2026-09-14 A-tier regression-test follow-up adds one or more tests per fixed finding, each exercising a normal path, the minimal input that triggers the defect, a boundary or recovery path, and the preserved original behaviour. F-03: `AiKnowledgeScopePicker.test.tsx` renders the picker under `TZ=Asia/Shanghai` at `2026-09-13T16:30Z` (00:30 local, where the UTC date and the local date disagree) and asserts the date scope defaults to the local day, plus `src/lib/localDateContract.test.ts`, which fails if `toISOString().slice(0, 10)` appears anywhere under `src/` other than `lib/tencentSigning.ts` and asserts the three scope sites call `todayISO()`. F-28/F-15: `storageAdapter.aiSettings.test.ts` drives `migrateAiSettings` against a mocked settings row (retired `deepseek-chat` migrates, a non-legacy model is left alone, a second pass is a no-op, a fresh install is seeded with `deepseek-v4-flash`), and `aiProviders.test.ts` covers the unknown-`builtIn` fallback. F-18: `RecordEditorPage.test.tsx` rejects a draft save and asserts the rendered `formatUiError` wording plus diagnostic id, that the raw error never leaks, that the message clears on the next success, and that the success path stays silent — this test caught a real gap, because the original commit captured the message in state but never rendered it. F-10: `src/preview/previewGuard.test.ts` covers the local-web happy path, the native-WebView refusal, the desktop refusal, and the host/parameter boundaries. F-19: `ocrService.test.ts` pins both that the retained `runPaddleOcrViaBrowserFetch` can still complete a full direct-connect job when driven explicitly and that `runPaddleOcr` still refuses on web. F-22 and F-21: `src/lib/dependencyHygiene.test.ts` and `src/styles/deadRuleCleanup.test.ts` guard the three-way package.json/lockfile/`manualChunks` removal, the `prosemirror-history` retention, and the dead-vs-live `.review-evaluation-*` split. F-20 and F-12 remain verified false positives with no change. Every new test was confirmed to fail against the pre-fix source (patched and restored byte-identically outside version control). The `e2e/voice-recall-production.spec.ts` provider assertion was updated from `deepseek-v4-pro` to `deepseek-v4-flash` to match the new seed. `npm run test` reports `168` files / `1053` tests passing, the Playwright suite is green, the Firebase Emulator suite reports `4/4` passing, and `npm run build` succeeds from a clean `dist/`.

## Voice Call Natural Conversation Freeze

- `docs/voice-call-natural-conversation-freeze-2026-09-14.md` is the active design baseline (`voice-conversation-design@1.0`); v1 uses user-first natural conversation and button-only interruption.
- Prompt version is `voice-conversation-prompt@1.0`; ordinary validation excludes live provider tests.

## Voice Recall Boundaries

- Voice recall is an `ASR -> LLM -> TTS` active-recall medium, not a video-call feature or a third learning truth system.
- `voiceRecallSessions`, `voiceRecallTurns`, and `voiceRecallLocalHistory` are local-only schema 21 stores. They never enter Firebase, ZIP/streaming/native backup, knowledge export, record transfer, or cloud mutation bookkeeping.
- Full restore clears transient sessions/turns but preserves pre-existing local history. Cloud pull/no-op sync preserves all three stores. Local history itself is not portable; durable preservation must go through an explicitly created journal record.
- `userMuted` and `systemCaptureGate` are independent. Navigation, backgrounding, and view unmount pause capture/playback without ending the session; recovery always resumes paused.
- `NativeVoiceCapture` is separate from `NativeAudioRecorder`, and the two Android microphone paths must remain mutually exclusive.
- The deterministic `voice-mock-cn@1` template is verified for automation. `voice-default-cn@2` defaults to Aliyun Paraformer ASR, the user's current LLM, and Fish Audio TTS, while the start workspace can independently select configured ASR/LLM/TTS profiles. The default three legs were verified against live services on 2026-09-09, but the template stays `candidate` until physical-device acceptance. Aliyun NLS 识音石 V1 is a separate candidate with a verified desktop/Node protocol lifecycle but no Android physical-device speech acceptance. Doubao profiles remain candidates: TTS 2.0 short-call playback passed on 2026-09-21, but ASR, small-model TTS, billing and long-session gates remain; never claim model, voice, quota, cost, browser-direct support, or streaming behavior from configuration alone.
- The natural-conversation voice reply chain clamps `max_tokens` to 224 by default and 250 globally. It sends no `thinking` option unless the selected provider explicitly declares `voiceThinkingMode: "enabled"`; reasoning deltas are never forwarded to TTS. Deterministic adapter tests cover this; live provider tests remain opt-in.
- Web permits only Mock, a user-controlled relay, or a profile explicitly validated as `browserDirectSupported`. Provider credentials and account-specific overrides remain device-local.
- Voice practice never writes FSRS or auto-rates a review card. A user-confirmed Coach transcript uses the existing `ReviewCoachOrchestrator.submitQuizAnswer` path with one answer per formal turn; a voice summary becomes a journal only after explicit confirmation.

## Review Annotation Boundaries

- Review annotations wrap the read-only review editor and never mutate `RecordBlock.contentHtml`.
- Drafts live only in `reviewAnnotationDrafts`. They are excluded from cloud sync, ZIP/streaming/native backup, knowledge export, and record transfer, and annotation writes must not mark a cloud mutation.
- An unscored draft survives component unmounts, navigation changes, reloads, and app restarts on the same device. A successful rating clears the matching `(recordId, reviewOccurrenceKey)` draft; undoing that rating restores the card but never restores the old annotation.
- Rating undo history belongs to the App-level `ReviewSessionRuntimeState`. It survives navigation while the App root remains mounted, but is not serialized into tab history, Dexie, backup, or sync.
- The current annotation editor supports browsing, pen/highlighter/eraser, basic shapes, text/input/select elements, color, width, opacity, and annotation undo/redo. Selection transforms, grouping, z-order editing, orphan repair, and full Excalidraw parity remain future work; do not describe them as implemented.

## Review Coach Boundaries

- `RecordBlock.contentHtml` is the only editable source of decision-block content. `DecisionBlock` is an index, not a second content copy.
- Formal review-coach writes go through `src/features/reviewCoach/repository.ts`; cross-entity workflows belong in `orchestrator.ts`, not `useAppData.ts`.
- Projections must be rebuildable from formal facts. AI responses and local execution caches are not truth sources.
- Record-level FSRS remains responsible for whole-record scheduling. Decision-block facts must not silently rewrite FSRS state.
- Stage 3 added block feedback, immutable history, tombstones, queue enrollment, exclusion/restoration, analysis notes, and manual legacy-comment association.
- Stage 4 calls only the quick feedback interpreter and preserves original feedback.
- Stage 5 adds the manually confirmed deep-analysis workbench, validated `SessionBlueprint` creation, and deterministic current/waiting/deferred task scheduling.
- Stage 6 adds the dedicated adaptive review page, Blueprint-constrained turn generation, independent question quality review, answer evaluation, hint/skip/invalid/defer/abandon dispositions, and atomic answer/outcome commits.
- Stage 7 schedules delayed verification through deterministic local policy, requires fresh retrieval questions, records retained/decayed outcomes independently from record FSRS, rebuilds block/effect projections from formal facts, and applies aging plus a two-verification streak cap to task selection.
- Stage 8 makes schema 11/16 migration transactional and retryable, retains confirmed legacy quiz/KnowledgePoint facts and record-level comments without automatic block binding, syncs every formal entity and tombstone, archives losing decision-block content conflicts, strips prompts/raw provider responses/secrets at export boundaries, and removes old Coach runtime tables.
- Stage 9 adds deterministic Playwright coverage at desktop and Android-narrow viewports plus isolated Firebase Emulator acceptance for namespace rules, bounded incremental writes, no-op replay, interruption recovery, and revision-based clock-skew convergence.
- The post-Stage-8 cloud audit keeps AI prompts, device-local backup paths, and knowledge-podcast rows/audio outside cloud and portable exports; restore preserves those local values transactionally. Ordinary no-op sync skips the cloud lock, and large read/write plans require explicit confirmation.
- App initialization may refresh due verifications, but must not select or replace the current task. Startup-created verification tasks use IDs and queue timestamps derived from the verification fact so two devices produce identical sync hashes.
- Cloud review-event history is append-only until a checkpoint plus event-tombstone protocol is designed. Do not delete remote events merely because local retention compacts old logs.
- Stage 6 quick-model calls use strict JSON and explicitly disable thinking. Controlled real-provider acceptance may use `https://api.deepseek.com` with `deepseek-v4-flash`; never persist API keys in source, tests, docs, logs, screenshots, backup, or sync data. Exception: the Firebase **web** configuration in `src/services/firebase.ts` (apiKey/projectId/appId) is a public client identifier, not a secret — it is designed to ship inside the web bundle and the APK, and access control is enforced by `firestore.rules` / `storage.rules` under `users/{uid}/**` (audit 2026-09-14, cross-cutting invariant #10). LLM/TTS provider keys remain device-local and are still fully covered by this rule.

## Cross-Cutting Checks

When changing decision-block or review-coach behavior, verify all affected paths:

- Dexie migration and repository invariants
- backup/restore and record-transfer round trips
- cloud-sync entity mapping and tombstones
- export privacy at cloud, ZIP, streaming, and native repository boundaries
- read/write quota estimates and no-op lock avoidance
- record deletion and mixed-record cleanup
- Desktop and Android narrow-screen interaction

## Verification

```powershell
npm run test -- --exclude "**/*.live.test.ts"
npm run test:voice-host
npm run test:e2e
npm run test:firebase
npm run build
git diff --check
```

Use deterministic mocks in automated tests. Real AI providers are limited to explicit, controlled acceptance runs and must never replace deterministic CI coverage.

The current automated acceptance baseline is `209` deterministic Vitest files / `1528` tests (the opt-in live files are explicitly excluded from ordinary acceptance), `92` Playwright tests collected across Desktop and Android-narrow projects (`90` passed and two platform-inapplicable assertions were skipped), and `4` isolated Firebase Emulator tests. The local host protocol gates also include two Node WebSocket tests and one Android OkHttp MockWebServer test; the Aliyun NLS real-account protocol lifecycle is an opt-in live gate. Physical Android keyboard/IME, system back, image gestures, real-device audio behaviour, NLS multi-turn/expiry/weak-network behaviour, button interruption, and controlled real-account Firebase quota checks remain manual release gates. See `docs/release-handoff-2026-09-21.md` for the current maintenance build and `docs/studyjournal-final-freeze-2026-09-15.md` for the historical closure run and `docs/second-audit-repair-acceptance.md` for R1–R15 and credential setup.

The 2026-09-19 editor clipboard closure confirmed the external-paste root cause: browsers commonly prefer `text/html`, while the editor's formula custom elements previously had empty contents and only a `data-latex` attribute, so an external input discarded the formulas even though `text/plain` contained LaTeX. `RichTextEditor` now preserves `data-latex` for in-app round trips and emits visible `$...$` / `$$\n...\n$$` fallback text inside the copied HTML. Native-selection recovery and formula boundary handling remain enabled; the temporary black-box probe was deleted. Coverage is `src/components/RichTextEditor.test.tsx` and `e2e/editor-clipboard.spec.ts` (desktop and Android-narrow). Right-click menus and drag-and-drop remain out of scope.

The clipboard follow-up also closes the mixed-structure edge: when an application-generated clipboard payload contains the ProseMirror slice marker and `record-*` nodes, in-app paste prefers that HTML Slice over the Markdown/plain-text fallback, so a paragraph plus collapse block remains structured even when formulas are in the same selection. Editable Ctrl+X uses the same serializer before deleting the selection; read-only content remains protected. The plain-text/HTML fallback remains readable for other applications.

Firebase Android resource values (`google_app_id`, `google_api_key`, sender id, project id, and the OAuth web client id) are public client identifiers required by the native Google sign-in/Firebase SDK; they are not LLM/TTS/ASR secrets. Authorization remains enforced by Firebase Auth and `firestore.rules` / `storage.rules`.

For local Stage 3 UI acceptance, run `npm run build`, start `npm run preview -- --host 127.0.0.1 --port 4177`, and open `http://127.0.0.1:4177/?preview=stage3`. This localhost-only query seeds an isolated `BFS Stage3 Preview` record with an overdue review, block feedback, and an analysis-queue item; it is gated out of normal URLs and native shells.

For Stage 4 UI acceptance, use `http://127.0.0.1:4177/?preview=stage4`. It adds a deterministic completed quick-model interpretation with diagnostics and confirmation controls without contacting an AI provider.

For Stage 5 UI acceptance, use `http://127.0.0.1:4177/?preview=stage5`. It seeds deterministic eligible blocks, an OCR warning, a partial analysis result, and current/waiting/deferred tasks without contacting an AI provider.

For Stage 6 UI acceptance, use `http://127.0.0.1:4177/?preview=stage6`. It seeds a deterministic in-progress task with one displayed, quality-checked turn; hints, answer submission, skip, invalid-question reporting, defer, and abandon controls can be exercised without contacting an AI provider.

For Stage 7 UI acceptance, use `http://127.0.0.1:4177/?preview=stage7`. It seeds one in-progress delayed verification plus retained and decayed history, then rebuilds block and intervention-effect projections without contacting an AI provider.

For a combined Review Coach showcase, use `http://127.0.0.1:4177/?preview=coach`. It renders the dashboard, adaptive training, and delayed verification in a localhost-only preview shell. Its controls use deterministic in-memory behavior and do not mount cloud sync or contact an AI provider.
