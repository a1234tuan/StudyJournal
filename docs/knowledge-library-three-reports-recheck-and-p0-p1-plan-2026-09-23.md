# 知识库三份审查报告复核与 P0/P1 修复方案

日期：2026-09-23。核对基线：本机 main / c9f4b68。状态：仅复核和方案，未实施产品修复。

## 1. 结论与范围

本次逐项裁决三份报告中 **5 条 P0、17 条 P1，共 22 条主张**。按实际数据风险、可达路径和现有产品约定重新定级，保留 **2 项 P0、6 项 P1** 进入修复方案；其余为误报、现有设计行为、P2 健壮性/维护问题或未证明的风险。不是把三份报告的等级照抄到待办。

报告代号：
- A：桌面的 knowledge-library-full-review-2026-09-23.md。
- Q：桌面的 Qw-审查报告：知识库功能全量复审（4 个提交，HEAD = c9f4b68）.md。
- D：桌面的 ds-knowledge-library-review-2026-09-23.md。

报告中的建议、曾运行的命令和“PROVEN”等标签均作为待核实材料，不作为本次操作授权。本次没有 fetch/pull、部署规则、调用付费服务、连接真实账号、改写真实备份、安装 APK/EXE、修改 AGENTS.md 或修复功能代码。保留了工作区原有未跟踪 docs/knowledge-library-review-2026-09-23.md，没有覆盖它。

后文源码路径均相对于 D:/StudyJournal-Source，行号对应 c9f4b68。证据区分：**探针验证**、**现有测试验证**、**静态确认**、**尚未复现/仅风险**。静态确认不等于真机验证，测试通过不等于问题已经修好。

### 进入方案的八项

- F01 / P0：原生备份仓库目的地隔离失效。来源 A-P0-01。
- F02 / P0：删除被 blocked 操作依赖的保留副本，破坏恢复证据并锁死确认。来源 Q-P0-1。
- F03 / P1：清空候选目标后，合法源库无法整库复制。来源 A-P1-01。
- F04 / P1：损坏云提交导致持续停同步，却没有可操作的损坏诊断/恢复流程。来源 Q-P1-1，**条件性问题**，不是主张绕过完整性校验。
- F05 / P1：知识库来源栈脱离导航会话，返回旧日志。来源 Q-P1-3。
- F06 / P1：导入命令大包随进度反复重写，完成后仍永久保留全文。来源 Q-P1-4。
- F07 / P1：旧草稿基线阻止退出；节点原位标题编辑的逃生入口尤其不完整。来源 Q-P1-5。
- F08 / P1：大纲滚动位置缺少库/专题归属，跨专题串用。来源 Q-P1-6。

## 2. 所有 P0/P1 主张的逐项裁决

### 2.1 报告 A

**A-P0-01：成立，保留 P0 → F01。**

src/services/autoBackupAdapter.ts:92 传入带 destinationId 的目录名，但 src/services/nativeAutoBackup.ts:203 起的 Desktop 分支丢弃该参数；desktop/preload.cjs:26、src/desktop.d.ts:28 没有相应契约，desktop/main.cjs:384 把仓库固定到 study-journal-backup。隔离探针传入 A/B 两个名字，实际写/读桥调用均只收到 manifest.json。src/services/nativeRepositoryBackupService.ts:355 共用保留列表，:258 的 GC 使用同一仓库，因此存在跨目的地读入、淘汰历史和删除资源的路径。

Android 的 NativeAutoBackupPlugin.java:843 仅凭 manifest.json + snapshots 就把所选目录当作仓库根，:856 在读写两种模式复用此判定。直接选中其他目的地的仓库根时，名称不匹配也可通过。Android 为静态确认，未做 SAF 真机复现。普通日志仍属设备级集合，不把它误报成此前不存在的“普通日志账号隔离承诺”。

**A-P1-01：成立，保留 P1 → F03。**

src/features/knowledgeLibrary/repository.ts:140 的清理只沿正式父子关系删除；节点仍活着时，它指向已清理父节点的移动候选可以合法保留。import.ts:49 对缺失目标使用 remap.get(...)!，产生 undefined 并被协议拒绝。探针执行真实 purgeLocalTrash 后，完整状态验证和 capturePortableKnowledge 均通过，但 prepareKnowledgeImport 抛“节点位置无效”。这是**复制失败，不是源库已经损坏或丢失**。

### 2.2 报告 Q

**Q-P0-1：成立，保留 P0 → F02，但不采纳“禁止删除所有 detached”的笼统修法。**

sync.ts:110 创建 detached 保留副本；repository.ts:122 只禁止删除 cloudLibraryId 非空的库。删副本之后，repository.ts:238 的 acknowledgeBlocked 因副本缺失拒绝执行；sync.ts:139 跳过 blocked，常规重试不会恢复。探针用真实仓库和一条指向副本的 blocked 行复现删除成功、确认失败。副本中才有的未采纳修改存在不可逆丢失风险；不能断言每次副本都是内容唯一来源。

并非所有 detached 都是同步证据：普通备份恢复副本也使用 detached。应该保护**仍被未解决操作引用的副本**。另外 schema.ts:12 没有 recoveryLibraryId 索引，原报告提出 where("recoveryLibraryId") 查询不能原样采用，否则 Dexie 会出现未建索引错误。

**Q-P1-1：异常场景成立，按条件性 P1 纳入 F04；“跳过坏提交继续”不可采纳。**

sync.ts:79 拉取连续日志，:88 在事务内验证；无效提交会回滚并保留 cursor。现有 sync.test.ts:42 对语义伪造包拒绝推进游标的测试通过。若同一坏提交一直存在，后续尝试确实反复失败，客户端没有损坏修复流程。正常提交、临时缺行/离线不应与持久损坏混为一谈。

这不是任意其他账号可注入的漏洞；需要本账号故障/恶意客户端、后台错误等条件。**拒绝坏包是正确行为**，缺口是诊断、止住无效重试、保全本地工作和显式恢复。默认库 registry 和历史证据有约束，不能承诺普通客户端可以安全跳过一个序号或自动换默认云库。

**Q-P1-2：报告的主要结论不成立，不纳入 P1。**

runtime.ts:36 的登录监听只切换 owner 和失效旧上下文，并不调用 connect。触发点是用户点击 CloudSyncButton.tsx:20 的全局同步；runtime.ts:24 在没有已绑定库时才连接。docs/knowledge-library-ux-follow-up-2026-09-23.md:11 明确记录了“移除独立入口，既有全局同步连接默认账号库”，KnowledgeSyncSettings.tsx 也作了说明。这比旧重构计划更接近当前已实现约定，不能用旧入口设计否定新行为。

本机/访客内容没有因此自动汇入。首次连接可能创建空云库、知识同步串行等待会延迟普通同步，这些代码事实存在，但“登录立即上传”“每次必耗时30秒”均不成立；30秒是超时上限，权限失败可能提前返回。保留为范围说明/响应性 P2，不重新增加启用流程。

**Q-P1-3：成立，保留 P1 → F05，证据为静态导航路径。**

App.tsx:286 保存来源数组，:1195 push，:565 在特定返回条件下 pop。commitNavigation(:392) 和普通切 tab/入口没有清理或校验其归属。日志 A → 知识库 → 切到其他 tab → 独立从“更多”打开知识库 → 返回，可消费旧的 A 快照。Web/native 的具体返回入口不同，本轮没有声称跑完浏览器/真机复现矩阵。

**Q-P1-4：成立，保留 P1 → F06；不采信未经测量的夸张倍数。**

import.ts:64 把所有命令保存在 sync.importSessions；repository.ts:209 每推进一条重写带完整命令数组的行，:203 还有普通同步状态写入，完成后没有收缩。探针确认已完成会话仍保留正文命令。若会话包含 N 条命令、正文总量 B，进度持久化产生 O(N×B) 级重复序列化/写入量；文本量随 N 增长时呈平方级增长趋势。这不是已测得“数十万倍”写放大，也不是已复现某款手机卡死。

**Q-P1-5：成立，保留 P1 → F07；实际比报告描述更窄也更危险。**

repository.ts:216 禁止覆盖旧草稿原始基线，是必要的并发保护。用户遇到旧基线草稿，在 beginEdit(:184) 的提示中选择不打开旧稿，页面仍开启新基线编辑，却没有处理旧草稿。之后 dismiss(:114) 必须先 saveDraft 成功才退出；探针确认不同基线保存必失败。back/leave 拦截继续阻止退出。

非原位编辑表单有“放弃草稿”；但 KnowledgeLibraryPage.tsx:292 的编辑表单仅在 !inlineEditing 时渲染，原位节点标题只有保存/取消，取消仍走失败的 dismiss。不能把弹窗中的“放弃草稿”当成所有编辑模式都有的出口。不得通过移除 saveDraft 的基线校验来修复。

**Q-P1-6：成立，保留 P1 → F08，静态确认。**

KnowledgeOutline.tsx:29 初始化共享 scrollTop，:36 的 effect 清理回写当前滚动；KnowledgeLibraryPage.tsx:269 添加引用后的跨专题导航没有清零/切换滚动归属。当前 navigation 只有一个 scrollTop，组件 key 重挂载不能给它增加归属，旧组件退出回调也可能覆盖新页面状态。这是 UI 导航状态串用，不是专题数据被合并。

**Q-P1-7：行为存在，但不足以作为当前 P1 bug，降为 P2 交互风险。**

KnowledgeMap.tsx:134 确为鼠标6px阈值，:147 松手提交；页面没有结构撤销入口。但 docs/knowledge-library-map-refinement-2026-09-23.md:9 明确改为节点主体拖动，并提供预览、非法落点拒绝、Escape/pointercancel 取消；因此不能继续按 R3 的旧把手/整理模式断言“实现违规”。补安全撤销有价值，但没有证据证明当前拖动必然误写，不把本次修复变成重做拖动交互。

**Q-P1-8：不成立，属于显式“新建”行为。**

KnowledgeLibraryPage.tsx:293 的空目标选项明确叫“新建收集节点”；已有节点可另选。反复选择新建产生多个同名根节点不是确定性引用去重失效。节点名称不是唯一身份，也不应擅自按“收集的日志”名称合并。

### 2.3 报告 D

**D-P0-1：原型污染结论不成立；宽松导航净化最多保留 P2。**

navigation.ts:23 的 Object.fromEntries 创建自有属性，mapSides 的值又被限制为 ±1；不是递归对象合并，也不是给 Object.prototype 写值。探针对 __proto__/constructor/prototype 输入确认 Object.prototype 未变，__proto__ 是自有数值属性。真实实体 ID 在协议和云导入中排除保留键；未找到报告所称“远端数据触发污染”的可达链。任意伪造 history 的无效 ID 防御仍可加强，但不能称已证实 P0 安全漏洞。

**D-P0-2：不成立，报告混淆了待提交 packet 与已确认远端镜像。**

sync.ts:153 的 after 是内存提交提案，不直接写入 readKnowledgeRemote 使用的表。无内容变化的命令仍需云端幂等回执；零 slots 的 receipt-only commit 推进 head 是允许的协议行为。firestoreTransport.ts:70 在事务中检查真实 head，再原子提交 head/commit/receipt；客户端只有 pull 后才删除相应 pending。

新增双设备探针让两个设备分别创建同一个确定性引用：第二台产生零 rows 提交。模拟 publish 提交前拒绝时，本地远端镜像没有该回执，队列保持 unknown；恢复发布后 pending=0，逐包 applyKnowledgeCloudPacket 校验通过，游标与服务端包数一致。firestore.rules:58、:77 静态允许这一零 slots 情形；本轮未运行 Emulator，不冒称真规则执行验证。不要删除这段逻辑，也不要把“当前内容相同”直接当作云端已接受。

**D-P0-3：事实冲突存在，但不是产品 P0，降为 P2 文档债。**

AGENTS.md:5 的 actual schema remains24 与后文 schema25 冲突，database.ts:368 和 schema.ts:3 的实际代码为25。旧验收数字/后续提交未入基线亦属文档滞后。无需据此修改迁移，也不能照报告建议保留“D12用例尚未执行”等未经重新核实的现状描述。本次只记录，不顺便整理工程文档。

**D-P1-1：长 JSX 行属实，属于 P2 可维护性，不是已证明的运行 bug。**

KnowledgeLibraryPage.tsx:319、:321 的巨型分支应另行重构，但“某个审查工具截断输出”不是产品故障。修复本次具体问题可以必要拆分局部组件，不立项全页重写。

**D-P1-2：不成立。**

sync.ts:122 的含义是把本地旧不可变证据补回 remote+pending 派生出的 working，而非只对远端做 GC。合并右侧 ...working.revisions 并未漏掉远端新增版本；applyKnowledgeCloudPacket 在返回前执行完整 validateKnowledgeState，不允许远端 revisions 指向不存在的实体。现有“stale pending restore”测试还明确要求保留 restore 的本地 revision。报告没有构造通过现有校验却失败的输入；不可把历史不可变证据简单替换为远端子集。

**D-P1-3：不成立，是保留冲突的预期语义。**

import.ts:51 和 repository.ts:199 的非 strict 导入用于保留候选；同一 session 的 command/entity ID 确定，进度与命令 hash 做核对。现有 import.test.ts 验证同一 session 重入不重复复制及候选保留。用户主动发起新的 session 本就应产生新副本，不能要求两次显式复制合并成同一实体。源库是 prepare 时冻结的快照，之后编辑源库不会自动混入旧会话。

**D-P1-4：成本事实成立，降为 P2，未证实当前功能失效。**

protocol.ts:270 的全量克隆有线性成本；与 F06 的持久化大包不同，没有实测证明其达到 P1 故障门槛。不要为性能猜测改成就地修改，从而破坏失败回滚、before 不变及确定性协议。

**D-P1-5：不能一概视为 bug，降为 P2 防御性改进；真实缺失目标已归入 F03。**

autoBackup.ts:27 在损坏数据库缺少 syncState 时会抛通用异常，诊断可以改进；但 repository.ts:59 建库时库行和 syncState 在同一事务加入，报告“库刚创建、状态尚未写入”的正常中间态不成立。几何计算、DOM closest、受 length 检查的 pop 不能仅凭非空断言定级 P1。损坏状态也不能按 dirtyGeneration=0 静默放行备份。

**D-P1-6：未证实 P1；保留 P2 代码健壮性。**

KnowledgeMap.tsx:22 使用 ref 保存侧向偏好，渲染期更新若干 ref 的事实存在；但页面按 workspace.id 重挂载、地图按 mapWorkspaceId 控制初始偏好。缺少明确的当前可达错误序列，不能只凭 useMemo 依赖数组表面形态断言地图已错位。未来变更 props/组件复用时需要专项验证。

**D-P1-7：明确误报。**

query.ts:33 已独立按 record.assets 从 assetMap 拼接 OCR，未依赖 recordToPlainText 的第二参数。页面传入 assets；启用“正文”搜索后 OCR 可命中。探针在同一 record 对象不变、资产 OCR 更新时仍命中新文本，亦排除了 bodyCache 掩盖更新的猜测。

**D-P1-8：命名冲突存在，降为 P2；不改导出协议。**

AiExportPage.tsx:23 的“知识库 JSON”实际为 AI 日志语料 JSON，helper 描述的也正是这些字段；完整知识组织的恢复包是另一条 v7 通道。可另行澄清标签，但并未证明当前入口承诺导出组织树后静默丢弃它。不能借改名问题把两种导出格式合并。

## 3. 原报告 P2 的核对及不纳入边界

以下只记录核对结果，不在本次修复清单中安排 P2 改动。

### 3.1 A 报告的三项

- A-P2-01：**静态成立的交错窗口**。repository.ts:84/:112 在异步读取后没有一直携带最初 generation 做末尾复核；storageAdapter.ts:2547 与 backup.ts:64 的多库恢复也没有完整的事务返回前代次校验。本轮未重跑报告中的交错注入探针，不引用对方“已复现”作为本轮结果。写入仍使用原 owner，不能升级为已证明跨账号读取。
- A-P2-02：**静态成立**。KnowledgeOutline.tsx:59 选择相邻按钮，:75 的缺失/回收站日志按钮 disabled，不可聚焦，方向键可能停在前一行。只记录，不混入 F08 的滚动修复。
- A-P2-03：**成立**。scope.ts:5 的 registry 校验弱于命令 ID 约束，规则也仅检查字符串等基础条件。官方生成的合法 ID 不触发；异常/旧数据防御问题不升为 P1。

### 3.2 Q 报告的分组 P2

- 云提交只约束状态与证据形状、缺少命令本体重演：**信任边界事实成立**；不能把同账号恶意客户端风险说成跨 UID 越权。改变该模型需独立同步协议设计，不以跳过校验修复。
- join/initialEmptyRemark 历史版本、按每个 group 扫 candidates：**代码事实成立**；不可达旧版本有存储成本，不等于可安全删除的垃圾，部分证据还供 pending/draft 基线使用。
- CloudSyncButton 的动态 import 失败被“普通日志”前缀承接：**成立**；正常知识同步已内部转换为独立 summary，不能扩大成全部知识错误都冒充普通日志错误。
- navigation.query 未用于持久搜索、KnowledgeResults 本地 query 随卸载丢失：**成立**；属于搜索状态体验。
- success 前缀分支、焦点 DOM 顺序依赖、部分 owner 切换 UI 状态未显式清理：**维护/健壮性观察**；旧命令仍有 assertContext 边界，没有据此确认越权提交。
- 渲染期 ref、220ms 单击判别、树祖先循环依赖无环不变量：**事实/设计约束**；前两者不自动等同 bug，无环已经是完整状态校验要求。
- 切库 scrollTop：并入 **F08**，不重复计数。缺失日志统一写“等待原日志同步”：**文案偏窄**。保留 knowledgeReferences 本身并非 bug，不能为了删除日志而自动销毁组织备注和回链证据。
- 四个复合索引只有声明未见查询：**文本查询确认**；是写入成本，不是数据正确性缺陷。
- 负向断言、日志被 gitignore 排除、没有独立 CloudSyncButton 测试、旧验收基线/个人绝对路径：**质量与可复核性问题**，不是产品 P0/P1。负向断言本身并非恒真，缺少某个测试也不证明产品有错；仅凭文件名不能断言所有相关端到端行为“零覆盖”。本轮不以报告声称的历史通过数作为现状。

### 3.3 D 报告的 P2 材料限制

D 的摘要写“8 项 P2”，但提供的正文从 P1-8 直接进入修复顺序，没有8条独立 P2 条目、触发路径和证据。摘要提到的布局/查询成本、mapSides上限、原生confirm、诊断导出属于可继续讨论的方向；不能据此编出8项“已确认 bug”。本次已核对其中与其他报告重合的地图/性能/诊断事实，未列出的其余项标为材料不足。

## 4. 修复方案：仅 F01–F08

### F01 / P0：把目的地隔离贯通到原生宿主

**修改面**：src/services/nativeAutoBackup.ts、src/desktop.d.ts、desktop/preload.cjs、desktop/main.cjs、Android NativeAutoBackupPlugin.java、nativeRepositoryBackupService 及其测试；调用方 autoBackupAdapter/currentKnowledgeBackupDirectory 的传参回归。

**实施决策**：
1. ensure/list/beginWrite/readText/readChunk/delete 的 Desktop IPC 全部显式携带 repositoryName；宿主验证是单段合法目录名，拒绝路径分隔符、..、绝对路径。保留最终路径位于所选父目录内的检查；对符号链接/junction 等实际路径边界增加测试。
2. beginWrite 创建会话时固定目的地根路径和绑定代次；append/finish/cancel 只能操作该会话，不在账号/目录切换后重算到新根。绑定变化要明确取消/拒绝旧任务，不能把新授权挪给旧会话。
3. Android 拆分“显式读取历史仓库”与“当前目的地写入”：不能仅以存在 manifest+snapshots 认领所选根。写入只允许确认为当前目的地的根，或在选定父目录创建匹配子目录；直接选中别的仓库根时拒绝并要求重选父目录，避免偷偷嵌套或写进旧仓库。
4. 旧固定 study-journal-backup 保留只读导入/明确选择入口；缺少可信目的地身份时不得自动并入新 manifest、覆盖或执行 GC。必要的仓库归属元数据须向后兼容，并使用 opaque destinationId，不用明文 UID 作为目录名。
5. GC、预览、最新快照、恢复、资产枚举与校验必须使用同一个已验证根，不只修 beginWrite。冻结备份确认只在目标实际校验成功后推进 capturedGenerations。

**验收**：临时父目录中的 A/B/guest 各写至少6份，manifest、最新快照、快照保留集合和共享内容哈希资产均不得互读/互删；A/B/guest切换、直接选择旧根/他人根、同名不相干目录、会话中重绑、非法路径、失败取消、旧包只读恢复。Desktop 必须有宿主文件系统级测试，Android 必须补 SAF 分支测试并保留真机确认门槛。禁止在用户真实备份目录做破坏性验证。

### F02 / P0：保护未解决操作依赖的保留副本

**修改面**：repository.ts、KnowledgeLibraryPage.tsx、sync.ts 相关状态展示与测试。

**实施决策**：
1. 在同一知识表写事务中查出指向该库的未解决 recoveryLibraryId 引用，再决定是否允许删除。schema25 下先用现有表的过滤扫描，不照搬不存在的索引。UI禁用只是提示，仓库守卫才是最终边界。
2. 保护条件不是 detached=true，而是“仍被未解决操作引用”。普通 detached 恢复副本仍遵守正常显式删库策略；确认完所有依赖后，副本可以按正常流程删除。
3. 检查同一受保护副本的 purge/编辑入口：完整保留证据不得通过先删内容再清空回收站绕过删库守卫。对受保护副本提供只读查看与“另存可编辑副本”，不把新工作覆盖到原始保全证据中。
4. 页面只有确认副本存在且 owner 正确才显示打开入口。缺失时显示“保留副本已缺失”，不静默回落其他库。
5. 处理已经受损的历史状态：允许先从用户明确选择的备份恢复可用内容；若无法找回，提供独立的“放弃此待确认操作”流程，明确说明未采纳内容无法保证恢复，逐条确认命令 hash/代次/副本仍缺失后再移除 blocked。不能自动补空副本后当作“完整内容已保留”，也不能自动清空整个队列。

**验收**：真实 sync 产生 blocked→尝试删副本被拒且全表不变；一个副本被多条 blocked 引用；确认一条不能解锁另一条；先清理/编辑不能破坏保全内容；无依赖 detached 可删；历史缺失提示和显式放弃；账号切换/并发确认/删除交错回滚；普通日志不变。

### F03 / P1：复制时保留缺失目标的移动候选

**修改面**：import.ts、KnowledgeLibraryPage 冲突详情/目标展示、import.test.ts 与备份往返测试。

**实施决策**：
1. 预先构造源实体以及所有未解决 position 候选所引用目标的确定性重映射。目标缺失也生成 session 范围内合法、稳定的目标 ID，但**不创建虚构节点**；ROOT_NODE 单独保留。
2. 正式位置仍必须映射到真实存在的目标；只有未解决候选可以指向该缺失身份。利用现有非 strict 候选语义保留它，不能映射成 undefined、静默改到根节点或删除候选。该路线兼容现有 value/候选校验，不新增云协议字段。
3. 展示“原候选目标已清理”，允许用户明确保留当前正式位置，或选择有效目标后以现有 generation/setToken 的 resolve 命令处理；不能自动消费候选。源库不修改，复制来源映射可供诊断而不改变正式学习事实。
4. session ID 重试必须复用相同映射和命令，不因缺失目标重复制造候选；正式树本身损坏仍拒绝导入，不把所有异常都降级。

**验收**：本轮 purge→capture成功→import失败探针转换为复制成功断言；正式位置未变、候选数量与待处理状态保留、重试幂等、导出恢复仍合法；目标存在/已删除但未清理/已清理、根节点、自环/后代目标候选、文本冲突同库并存均覆盖。需加云包与 Emulator 路径测试，不能仅验证本地导入。

### F04 / P1：损坏提交的安全停止与恢复流程

**修改面**：sync.ts、firestoreTransport.ts、domain.ts 同步诊断状态、runtime.ts、KnowledgeSyncSettings/需要处理界面及隔离云测试。

**实施决策**：
1. 区分网络/超时、暂缺行、格式/语义不合法、回执不符、历史倒退；只对稳定可辨识的损坏记录持久状态，不能第一次网络失败就判损坏。
2. 错误事务回滚后，另开带 owner/generation/cursor 基线复核的短事务记录失败序号、类别、包指纹/内部诊断号和最后可信游标。不存令牌或正文到诊断文本，不推进游标，不写部分坏状态。
3. UI提供重试、导出当前可信本机库、另存 detached 本机工作副本和明确的修复说明；有 pending/draft 时先保存原状态及队列关系，不在恢复路径里丢弃。反复读到同一坏包时显示同一事件，避免重复创建恢复库和无限自动重试。相关保留副本继承 F02 的保护。
4. **同一默认云库恢复同步需要可信历史修复**。当前规则下客户端不能改写 immutable commits/revisions；因此生产修复只能在另行授权的管理流程中从可信备份核对并修复损坏历史，再由客户端从最后可信游标完整校验重拉。先在 Emulator 的隔离数据上验证该闭环，不能用“跳过下一条”冒充完成。
5. 不引入新默认云库、不清空本地队列、不放宽规则、不把整个库标为已同步。普通日志仍走原协议；知识库错误应清晰单独汇报，不因损坏静默触发普通数据恢复。

**验收**：坏包与连续后续包、坏包位于批次中间、临时缺行后恢复、重复坏指纹、回执冲突、修复后重试、账号切换、pending保全；坏历史期间游标不越过坏提交，修复后才收敛。管理员修复未获授权/未执行时，只能交付“安全停止与本机恢复”，不能宣称生产云库已修复。

### F05 / P1：来源记录与一次导航会话绑定

**修改面**：App.tsx 的导航提交/来源栈/系统返回，以及现有 webNavigationHistory 测试接线；不新增平行路由体系。

**实施决策**：
1. 来源项携带 owner、导航会话/入口标识及目标知识库位置；仅“当前记录进入知识库”的成功导航才压栈。导航被编辑守卫拒绝时不留幽灵来源。
2. “知识库→关联日志→返回知识库”属于同一来源链，保留；显式独立入口、无关跨 tab离开/新导航会话、账号改变则终结/重置相关链。不能在任何离开知识库时无条件清空，否则会破坏合法日志往返。
3. 消费来源前校验属于当前链且目标仍可用，失效按正常页面返回，不回放任意旧全量 NavigationState。只有导航实际提交成功才 pop/恢复滚动。
4. Web history恢复与Android/桌面Back共享同一来源规则，避免浏览器后退与内存栈双消费；数组须有生命周期上限/明确清理点。

**验收**：日志A→知识库→日志B→返回→A；知识库中切tab后从更多独立进入不回A；来源日志删除、账号切换、编辑拦截、反复进退、Web后退/前进及系统Back。断言 route/recordId/scrollY 和栈大小，而不是只看页面标题。

### F06 / P1：导入数据与进度分离，完成后释放正文

**修改面**：domain.ts、schema.ts、database.ts、repository.ts、import.ts、KnowledgeSyncSettings 及迁移/恢复测试。

**选定设计**：不用另一种 knowledgeSyncState 特殊主键藏大包，也不把未执行导入步骤伪装成可上传的 knowledgeCommands。增加独立的本机导入会话元数据表和逐步命令表；当前基线为25，若开工时未被其他工作占用，使用 **schema26**。这是本方案唯一明确需要数据库结构迁移的子项，须作为单独评审/回归批次；本次没有升级数据库。

1. 会话行保存稳定session/source/target/hash、total、next、状态；命令表以 libraryId/sessionId/ordinal 存单条命令。prepare冻结源快照并逐条保存，正式状态仍由原命令协议产生。
2. 执行一条命令、写正式库/上传队列、推进小型进度行在同一事务完成；避免每步重写全部 commands 或其他历史会话。并发继续同一会话需校验 next/hash。
3. 完成后删除该会话的全文命令行，保留小型完成凭证以保证旧session重试不重新复制。取消需用户明确选择，不能删除已经产生的正式实体/队列；来源被删后仍能按已冻结步骤继续。
4. 迁移 schema25 importSessions：活动会话逐步搬迁，完成会话只留下完成凭证；旧字段只在迁移成功后清除。升级失败整体回滚。完整删除目标库时清理其导入状态；不因删源库清掉目标的活动任务。
5. 新表为device-local运行数据，不进普通云同步，不混入便携知识正式内容。若备份恢复不恢复进行中任务，应明确只能还原已落库内容；不能悄悄把“完整恢复”扩展为自动继续复制。

**验收**：旧25库升级（空/进行中/已完成）、中断恢复、多次resume/prepare、双窗口竞态、源库修改/删除、账号切换、目标库删除、完成凭证重试；用存储写调用捕获对比100/500/1000步的序列化字节数，确认进度写入不再随全部剩余正文线性放大。另测墙钟/内存但不预先虚构秒数门槛。协议层全量clone的P2不在此批顺便重构。

### F07 / P1：保留草稿保护，同时提供可靠退出

**修改面**：KnowledgeLibraryPage.tsx、KnowledgeTitleInput 的取消接线、repository.ts 仅必要的明确草稿操作接口及组件测试。

**实施决策**：
1. 打开旧草稿时区分“继续原基线”“保留旧稿并另开编辑”“明确放弃旧稿”；不能只在confirm取消后默默创建注定写不回去的新基线编辑。允许保留的第二份草稿需独立草稿身份，不覆盖同键旧基线。
2. dismiss保存失败保留输入并给出可达的“重试保存”“复制/保留当前文本”“确认放弃本次编辑后离开”；原位标题和弹窗必须具备同等退出能力。复制失败/存储失败不能伪报已保存。
3. 明确用户确认放弃的范围：本次内存输入与原持久草稿分别说明；账号变更后不能借旧上下文删除新owner数据。被拦截的页面导航要在用户选定处理方式后重新提交一次，不能连续触发重复返回。
4. 原命令 expectedRevision、saveDraft 原始基线校验保留；成功保存使用旧基线检查，陈旧时让用户比较内容，绝不自动重基线覆盖正式内容。

**验收**：旧基线草稿+新正式版本、拒绝打开旧稿、原位标题cancel/Escape/Back/切tab、弹窗稍后继续、Quota/事务失败、离开后重开恢复、IME、账号变化。每条断言输入是否保留/明确放弃、草稿归属、是否能退出、正式内容不被旧回调覆盖。

### F08 / P1：大纲滚动状态绑定库和专题

**修改面**：navigation.ts、KnowledgeOutline.tsx、KnowledgeLibraryPage.tsx、App导航patch和web history净化测试。

**实施决策**：
1. 给 scrollTop 增加 library/workspace 归属或等价的有界按专题状态。选定最小方案：单个位置+归属标识；仅归属匹配才恢复，不匹配置0。不把设备导航状态加到数据库/云协议。
2. 更新滚动时传入原组件的library/workspace，父级只接受当前目标匹配的回调；卸载cleanup不能把A的位置写进B。
3. 所有入口统一同一辅助流程：首页打开专题、添加日志后跳转、搜索命中、切库、保留副本、日志往返、Web history。未知旧history安全落到0；同专题日志往返保留原位置。
4. 真实DOM scrollTop、虚拟列表start与 React 状态一起复位，不只写navigation字段；选中节点的reveal仍保持有效。

**验收**：长A滚到底→短B；A添加日志到B；同专题打开日志后返回；切库有同名专题；组件退出回调晚到；大纲/导图切换；旧history。断言首屏行ID、DOM scrollTop和保存归属，不以截图“看起来在顶部”代替。

## 5. 顺序、兼容性与验收门禁

1. **第一批：F01 + F02止住数据风险。** 两者可独立提交；必须先有回归再改实现。F02的受保护副本规则为F04复用。
2. **第二批：F03 + F07解除正常操作阻断。** 保留冲突和草稿证据，禁止用删除校验/证据解决表面异常。
3. **第三批：F05 + F08修正导航上下文。** 共享导航边界一起评审，组件和真实浏览器都验收。
4. **第四批：F06单独迁移。** 先固定schema25迁移夹具与幂等契约，再升级下一可用schema；不可和无关性能重构混在一个提交。
5. **第五批：F04损坏恢复闭环。** 安全停止/本机保全先验证；可信管理端修复只在隔离环境演练，生产修复另行授权。不要为了“自动继续”破坏连续历史。

每批执行针对性单测→相关集成/浏览器测试；收敛后串行执行非live全量Vitest（有界worker）、TypeScript、生产build、相关知识库四组e2e、Firebase Emulator。不要并发跑大型构建和全量测试后把资源竞争超时直接认作产品bug。原生路径另有Electron宿主文件系统测试和Android SAF验收；浏览器narrow不能替代真机。

必须回归：未知publish结果先对账receipt；零slots回执提交；remove-wins+显式restore；generation/setToken部分候选处理；知识/普通日志同步隔离；空知识包创建新detached而不清空旧库；当前scope备份范围；SJ-AUD-01生命周期写入边界；所有UI错误走现有安全格式化。新schema运行表不得变成云同步事实。

最终验收记录分别列“本机自动化通过”“生产规则是否部署”“宿主/真机是否通过”“用户是否验收”，不合并成一句“已全部完成”。本方案不授权安装、生产修改或有偿服务调用。

## 6. 本轮实际执行与证据

- 本轮新增隔离探针：2个文件 / **8条通过**。其中5条确认问题的可观察现状，3条验证原型污染/OCR/伪造回执主张不成立。通过的是“当前行为与复核结论一致”，不是产品修复回归已通过。
- 现有定向测试：**13个文件 / 76条通过**，覆盖知识库12文件75条及Desktop适配1条。没有用三份报告声称的全量1802/其他数字替代本轮执行数。
- 首次探针有2条夹具错误（注入状态未同步epoch、日志asset缺少合法字段），修正夹具后8条通过；未把夹具失败计为产品缺陷。
- 未运行本轮全量Vitest、tsc/build、Playwright、Firebase Emulator或Android/Electron真机测试；本轮只写文档/审计夹具，不变更产品实现。

可重复命令（仓库根目录）：

~~~powershell
npx vitest run --config output/knowledge-review-recheck-2026-09-23/vitest.config.ts --reporter=verbose
npx vitest run src/features/knowledgeLibrary src/services/nativeAutoBackup.desktop.test.ts --maxWorkers=2 --minWorkers=1
~~~

探针：output/knowledge-review-recheck-2026-09-23/recheck.test.ts、cloud-recheck.test.ts、vitest.config.ts。
日志：同目录 probes.log、existing-tests.log。output为本机审计产物，受gitignore影响，不当作已提交的团队证据；若进入实现，应把相应正确契约断言迁入正式测试目录并随修复提交。

**原复核交付边界：以上为实施前核实与计划，执行数及“未修改实现”描述仅指该次复核。**

2026-09-23 后续：用户已授权分阶段连续修复。实施、schema26 迁移与最终验收结果见 `knowledge-library-p0-p1-remediation-2026-09-23.md`；不应再将本文的实施前状态当作当前代码状态。
